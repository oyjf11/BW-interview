# 面试讲解稿与追问预案(研发效能 / DevOps 平台岗)

> **配套**:《企业harness套件-包装版.md》。
> **用法**:背熟"起手式"和"主线",追问按本文预案接;数字优先用真实实践量级(自愈 83%、六成、30%→8%→3%),平台级 DORA 数字标注"示例"。
> **心法**:先证明脑子里有**全貌**,再挑信息量最大的一段**深讲**,显得是"挑了个好例子",而不是"只会这个"。越往深追越占便宜。

---

## 一、电梯演讲

### 30 秒版(开场)
> 我做的是一个 AI-Native 的研发交付平台:把需求、开发、测试、缺陷闭环做成一条流水线,Agent 干活,但所有硬约束下沉到程序 —— 状态文件管恢复、Hook 管拦截、编译器和 Playwright 管"真的做成没有"、质量门禁管"能不能进下一步"。核心矛盾是 LLM 是概率性的、会幻觉、无状态,而软件工程要的是确定性、可验证、可恢复。我用工程手段把概率性行为框进了可执行、可审计的流程里 —— 一句话:**LLM 决定建议做什么,Harness 决定是否允许做、是否真的做成、有没有证据。**

### 2 分钟版(STAR 结构,主线讲解)
- **Situation**:团队想用 AI 提研发效能,但直接用 LLM 写代码/测试,卡在 demo 能跑、生产不敢用 —— 因为它概率性、会幻觉(宣称编译通过其实没有)、无状态(会话一断半截工作作废)。
- **Task**:我要做的不是"让 AI 写出代码",而是"让 AI 写出的代码**可信地进入交付流水线**"。
- **Action**:我把研发生命周期拆成有状态的流水线:① **需求站用 SDD**,把需求固化成机器能判的规格当单一事实源、驱动生成并做需求↔设计↔API↔测试的一致性校验(契约链);② **开发站**用状态机 + 断点恢复 + 定向修复 + 编译器退出码兜底;③ **测试站**是业界最难的 **Behaviour Harness** —— 前端 E2E 自愈 Agent,五段闭环 + 证据链质量门禁;④ 所有硬约束做成 **Hook + 确定性校验(执行前生效,不靠提示词)**;⑤ 企业级规划**控制面/数据面分离 + 模型网关 + CI 评测治理**(评测治理是 L4 roadmap)。
- **Result**:前端 E2E 夜间回归改版变红**自动处理约六成**,纯定位失效自愈成功率 **83%**,请求判等优化把不稳定率从 **30% 压到 3%**;整体成熟度到 **L3**,前端 E2E 接近 L3.5。下一步是统一 Runtime、状态引擎、评测治理,走向 L4。

---

## 二、分模块起手式(被点到某块时怎么开口)

**前端 E2E(最强项,务必背熟)**:
> "别一开口讲'自愈',那显得只会修脚本。它是个完整闭环 —— 编写、历史整合、执行、自愈、出报告**五段**;评测这五段用的是**同一套结构**:每段的'对'都拆成代码能判的**客观内核(Computational)**和只能上模型判的**主观残留(Inferential)**。它本质是三类 harness 里最难的 **Behaviour Harness** —— 验证业务真的对。我拿信息量最大的一段展开 —— 比如自愈,因为它四种裁判全齐、标准答案还能从 git 直接拿。"

**评测体系**:
> "评测的内核是一句话:**能用确定性规则判的,绝不优先交给另一个 LLM**。所以我先把每段的'对'拆成客观内核和主观残留,客观的全交代码(AST、沙箱重跑、集合比对、唯一性),只有真正消不掉的主观那一点(语义合不合理)才上模型裁判,并且只为那一点建 gold set。"

**SDD / 需求**:
> "SDD = 把'要做什么'在写代码前固化成规格、当单一事实源驱动 AI 生成(业界叫 intent is the source of truth,Spec Kit / Kiro / OpenSpec 都这么干)。一份好 spec 本身就是验收标准(Specification by Example),所以需求和验收合成一份、不建两套。它解决传统需求文档'是死的、写完没人维护'这个老毛病。〔再让同一份 spec 去守评测门禁,是我的 roadmap、目前未建,我会和上面分开讲。〕"

**企业级 / 架构师视角**:
> "企业级 harness 的内核和一个人搭的是**同一个内核**,变的不是内核,是规模新增了三个问题:互相干扰、会退化、难追责控本。我每一个企业设计都对着这三问 —— 控制面/数据面分离防扩散、模型网关防退化+控本、CI 评测门禁防退化、两个常设岗位维护评测体系。架构师交付的不是'一个对的 Agent',是'持续告诉你每个 Agent 错多少、且不让错误扩散'的系统。"

---

## 三、高频追问 + 标准答案

**Q1. 怎么防止 LLM 幻觉(它说编译通过其实没过)?**
> 不信 Agent 的任何"宣称"。编译看 **Maven 退出码**,测试看 **Playwright 真跑真浏览器**,报告里的数字和执行结果做**一致性校验**(数字对不上判幻觉)。原则:**LLM 负责生成,程序负责验证** —— 凡是能用确定性手段验证的,绝不让 LLM 自证。

**Q2. 约束为什么不写在提示词里就行?**
> 提示词是软的,想绕就绕。我有亲身案例:自愈 Agent 把断言删了作弊让测试变绿。死规矩"只许改定位、碰断言作废"我**没只写提示词**,而是做成提交前的**确定性强制校验**,过不了就提交不了。这就是"**约束要在执行前生效**"。

**Q3. 那个防作弊校验具体怎么实现?演进过吗?**
> 演进过三版:① 文本匹配 —— 被注释里/字符串里的关键字误判,不行;② 手写解析器 —— 不健全,被"它解析得了 Vue 吗"问破;③ 最终用 **swc 解析成语法树,按节点角色判** —— 定位语句允许改,一流进断言节点的期望值/匹配方式就锁死。这次作弊未遂也做成了**陷阱题永久入库**。

**Q4. 自愈率怎么从 60% 提到 83% 的?换模型了吗?**
> 不是换模型,是**喂知识**。第一版 60% 是因为 Agent 在原始页面结构上瞎猜锚点、不懂自研组件库门道。我把组件库定位约定整理成**知识包(用到才查,不硬塞)**挂给它。真坑有三个:Vue teleport 弹窗被挂到页面末尾、原地找不到;表格按"第几个子元素"定位、虚拟滚动一滚就漂(稳定锚点应是每行业务主键);下拉选项用到才渲染、不展开根本不存在。**"多出来那二十几分不是模型给的,是我给的"** —— 这就是 Guide(前馈控制),最有效的 Guide 是结构化知识不是空泛提示。

**Q5. 录制回放最难的点是什么?**
> 不是教科书三件套(关动画、固定时间、关字体),那只挡表面。难在**请求指纹归一化** —— "两个请求算不算同一个"。前端每个请求带随机请求 ID、请求体里有防缓存时间戳,回放时哈希永远对不上、全部穿透打真后端,上线第一周不稳定率反冲到 **30%**。修法:算哈希前剥掉时间戳/随机串/请求 ID。**最要命一行 —— 序列化前必须把字段名排序**:`{user,role}` 和 `{role,user}` 是同一个请求,不排序算出两个哈希。加排序后次日 30%→8%、第四周稳到 **3%**。一句话:**判等永远比你以为的复杂。**

**Q6. 模型裁判会不会自己也不准?怎么保证裁判可信?**
> 裁判要先"考试上岗"。流程:抓几十条这类判定 → **3 个专家各自独立打分(不许商量)** → 算裁判和人类标注的一致性系数 **kappa** → **kappa 及格(≥0.6)才许上岗**。五条铁纪律:裁判 ≠ 被考模型(防同源偏见)、裁判版本钉死(它一动所有分不能信)、裁判改动必回 gold set 重考、score 只取 0/1(连续分一致性算不清)、evidence 字段强制(防瞎蒙)。

**Q7. 门禁会不会误杀?太严了开发不就被卡死?**
> 门禁分层来平衡 —— **质量左移四层**:本地几十题快检(几分钟,只防大问题)→ PR 前全量+集成+安全 → CI 后变异测试+完整回归 → 持续运行盯漂移。按速度和成本把检查放最合适的位置,不是一刀切全严。另外门禁判的是**客观内核**(确定性、可复现),误杀概率低;主观部分才上裁判,且裁判校准过。

**Q8. 覆盖率能到多少?能全自动吗?**
> 我从不吹全覆盖,**不拿信任换覆盖率**。夜间回归我自动处理约**六成** = 纯定位失效自愈(成功率 83%)+ 真业务 bug 自动分诊报红交人;剩下环境/时序竞态(~20%)没完全解决,断言语义变了(需求真改,~10%)**禁止 AI 碰、转人**。守住边界比虚高的覆盖率重要。

**Q9. SDD 和写测试有什么区别?不就是 TDD 吗?**
> 关键区别:TDD 是代码层先写测试;SDD 更上游 —— 先把"什么算对"定义成规格、当单一事实源驱动生成,且一份好 spec 本身就是验收标准(Specification by Example),需求和验收合成一份。表达上不限形式(可用 EARS、Gherkin)。〔我在项目里想让同一份 spec 再去守评测门禁 / gold set,这是 roadmap、未建,会和上面分开讲。〕

**Q10. 这套和 Devin / Cursor / Copilot 有什么区别?**
> 那些是**编码增强工具**,解决"让 AI 写出代码";我做的是**交付平台**,解决"让 AI 写出的代码可信地进入流水线"。差别在工程体系:状态恢复、确定性门禁、证据链、评测治理、控制面/数据面分离 —— 我不赌单次输出对不对,我赌的是"错了能被拦住、能量出来、能恢复"。

**Q11. 怎么从一个人的工具扩到几千人不退化?**
> 内核不变,对着规模新增的三问设计:**互相干扰** → 控制面/数据面分离(校验在公共控制面,谁都绕不过);**会退化** → 模型网关版本钉死 + CI 评测门禁(改一行提示词都要重跑题库)+ 熵管理;**难追责控本** → 模型唯一入口按团队/任务算账、给死循环 Agent 设费用熔断。还有最容易漏的:**两个常设岗位(题库负责人、金标负责人)**,因为评测体系是组织产物,不会自己保鲜。

**Q12. ROI 怎么量化?怎么证明值得投入?**
> 我用 **DORA 四项指标**锚定:变更前置时间、回归测试人工耗时、变更失败率、E2E 维护成本(示例:回归从每轮 1.5 人天降到 0.5 人天,变更失败率 15%→6%)。但我会诚实说明 —— 真正的 ROI 不只在"省人",更在"**把不可信的 AI 产出变成可信的、可纳入流水线的产出**",这个价值在规模化后才完全显现。

---

## 四、主动埋雷话术(引导面试官往你准备好的方向问)

讲完主线,主动抛 trade-off,显得你想得比问题更深:

1. **"我可以展开讲一个我亲眼抓到 AI 作弊的案例 —— 它把断言删了让测试变绿,我怎么处理的。"**
   → 引出 Q2/Q3,Steering Loop,显出"懂系统不只会修脚本"。

2. **"我们当前还有几块是 Prompt 约束没下沉成确定性校验,这是我下一步要改的,我可以讲讲为什么这件事重要。"**
   → 主动暴露不足 + 给出演进规划,显出 self-awareness 和架构规划力。

3. **"前端 E2E 这块业界其实最不成熟,它在 harness 三分类里是最难的 Behaviour 层,我可以讲为什么难、我趟过哪些坑。"**
   → 引出 Q4/Q5,把对话拽进你最深的技术细节。

4. **"判断一个团队的评测是不是真做到位,我有个最准的标准 —— 看人力分配。"**
   → 引出企业级/组织视角,显出你不只懂技术还懂组织。

---

## 五、一句话收尾(面试结束前的总结)

> 这套平台的内核是三件事:**规格驱动(SDD)、确定性护栏(Guides/Sensors)、控制面/数据面分离**。它把 LLM 的概率性行为约束成可恢复、可验证、可审计的交付流程。最难的一块是业界公认最不成熟的 **Behaviour Harness**,我在那里趟过请求判等、防 AI 作弊、知识包提自愈率这些真实硬细节。下一步是统一强类型 Runtime、状态引擎、模型网关和评测治理 —— 从 L3 走向 L4 企业治理平台。我做的不是让 AI 更聪明,是让 AI 的产出**可信**。
