# OpenCode 企业级研发套件要点总结

## 1. 项目定位

这是一套运行在 **OpenCode** 上的企业级 AI 研发 Harness。

它不是简单的 Prompt、Agent、Skill 集合，而是把研发过程拆成：

```text
需求分析
→ 方案设计
→ 任务拆分
→ 代码开发
→ 编译验证
→ 测试设计
→ 测试执行
→ 缺陷回流
→ 定向修复
```

核心目标是：

> 用流程、契约、状态、权限、门禁和证据，把 LLM 的概率性行为约束在可执行、可恢复、可验证的软件工程流程中。

---

# 2. 基础能力架构

## Command、Agent、Skill 三层

| 层级          | 作用                                   |
| ------------- | -------------------------------------- |
| Command       | 用户入口、参数解析、场景路由、流程启动 |
| Agent         | 上下文管理、阶段推进、多 Skill 编排    |
| Skill         | 领域知识、脚本调用、文档或代码生成     |
| Rule          | 行为规范和场景约束                     |
| Hook / Runner | 确定性拦截、状态、重试和质量门禁       |

当前规模：

- 27 个 Command
- 24 个 Agent
- 90 个 Skill
- 9 类 Rules

## 三种编排模式

1. **直接委派**

```text
Command → Skill
```

适合边界明确的短任务。

2. **Agent 编排**

```text
Command → Agent → Skill
```

适合需要动态判断和多阶段执行的任务。

3. **自包含流程**

```text
Command → Step1 → Step2 → ... → StepN
```

适合长工作流、状态恢复和质量闭环。

---

# 3. 需求侧设计

需求侧目标是：

> 把模糊想法或正式需求转化为经过澄清、评估、确认和一致性校验的结构化设计输入。

## 双入口

### `/it-vibe`

面向自然语言想法：

```text
自然语言
→ 方法论路由
→ 只读探索
→ 2～3 个方案
→ 用户确认
→ 明确需求或方案
```

支持九种方法论：

- RCA 根因分析
- MVP 迭代
- 减法优先
- 深度探索
- Working Backwards
- 数据驱动
- 结构化探索
- 多 Agent 赛马
- 流程精简

探索阶段禁用 `Write/Edit`，防止 Agent 未理解需求就直接修改代码。

### `/it-design`

面向正式 US 需求：

```text
US 需求
→ 需求澄清
→ 项目理解
→ 技术方案设计
→ 文档评估
→ 一致性校验
→ 用户确认
```

主要产物：

```text
requirement-analysis.md
design_document.md
design_api.yaml
design_database.sql
```

关键门禁：

- 复杂需求必须用户确认
- 设计文档必须通过 Evaluator
- 需求、设计和 API 契约必须一致
- 用户确认后才能移交开发

---

# 4. 编写侧设计

编写侧负责：

```text
设计产物
→ 任务拆分
→ 任务校验
→ 代码实现
→ 文件自检
→ 编译验证
→ 定向修复
```

## `it-task-initializer`

将设计文档转成 `tasks.md`。

每个任务包含：

- 任务编号
- 功能点
- 涉及文件
- 涉及接口
- 涉及模块
- 依赖关系
- 当前状态

任务生成后做三方覆盖校验：

```text
设计功能点 ↔ tasks.md 功能点
代码文件清单 ↔ tasks.md 涉及文件
API paths ↔ tasks.md 涉及接口
```

遗漏任务自动补充，最多修复 3 轮。

## `it-developer` 三模复用

同一个 Agent 支持：

| 模式         | 场景                     |
| ------------ | ------------------------ |
| FULL         | `/it-dev` 完整开发       |
| COMPILE_ONLY | `/it-build-fix` 编译修复 |
| TARGETED_FIX | 质量闭环中的定向修复     |

当前通过 Prompt 参数识别模式，长期应改为结构化参数。

## 开发状态机

```text
[ ] 待执行
[~] 执行中
[x] 已完成
```

会话中断后：

- 优先恢复 `[~]`
- 再选择依赖满足的 `[ ]`
- 已完成任务不重复执行

## 开发侧关键能力

- Git 分支准备
- 设计文档前置全读
- AUI、Jalor、Starling 技术栈路由
- 任务依赖调度
- 涉及文件完成自检
- Maven 单模块和多模块识别
- `settings.xml` 感知
- 编译失败最多 5 次自愈
- 定向修复限制文件和方法范围
- 禁止绕过 Git Hook、修改 lint 规则或跳过任务

---

# 5. 测试侧设计

核心原则：

> 测试用例是设计文档的派生品，测试验证的是设计与实现之间的一致性，而不是重新分析需求。

## 测试输入

| 测试类型 | 输入                                             |
| -------- | ------------------------------------------------ |
| 测试点   | `requirement-analysis.md` + `design_document.md` |
| API 测试 | `design_api.yaml`                                |
| UI 测试  | `design_document.md` + `ui/` 设计稿              |
| 后端测试 | 设计文档中的功能和实现方案                       |
| DFX 测试 | 性能、安全等设计内容                             |

---

# 6. 前端 E2E 测试设计

入口：

```text
/it-test-design
→ ui-test-designer
→ ui-test-design.md
```

七步流程：

1. 检查或生成测试点
2. 读取产品设计文档
3. 提取完整页面 URL
4. 设置手动登录模板
5. 数据预检
6. 生成正向、边界、异常、交互用例
7. 输出测试设计文档

## 动态指令系统

测试用例不直接写死数据，而是使用：

```text
[DYNAMIC:DATE:CURRENT]
[DYNAMIC:DATE:CURRENT-7DAYS]
[DYNAMIC:SELECT:FIRST_VALID]
[DYNAMIC:CONFIG:base_url]
[DYNAMIC:TEXT:RANDOM:10]
```

运行时再根据环境、页面和时间解析真实值。

主要解决：

- 日期过期
- 多环境 URL 不一致
- 下拉值不同
- 测试数据冲突
- 用例依赖固定环境

---

# 7. 前端 E2E 测试执行

入口：

```text
/it-test-ex
→ ui-test-ex-agent
→ uitest-runner
→ Playwright MCP
```

## 执行阶段

```text
Prepare
→ Parse
→ Execute
→ Report
→ Review / Increment
```

## 分批执行

每 10 个用例一批：

```text
执行一批
→ 立即写 state.json
→ 自动执行下一批
```

中断后从上次批次恢复，不需要重新执行全部用例。

## `state.json` 保存完整上下文

不仅保存执行进度，还包括：

- baseUrl、host、port、protocol
- URL 来源
- Hash / History Router
- Vite、Vue、Vue Router 版本
- 已完成用例
- 失败用例
- 执行结果

## 原子操作

Markdown 步骤被转换为独立 Playwright 操作：

```text
goto
fill
click
wait
assert
screenshot
```

每一步保存：

- 执行状态
- 错误
- 耗时
- `raw_js_steps`
- 截图

---

# 8. E2E 质量门禁

关键机制：

- 语义元素定位优先
- 相似度低于阈值拒绝误操作
- 关键断言必须截图
- 截图后检查文件存在且非空
- 截图缺失自动补拍
- 证据不完整时阻断报告生成
- 路径统一处理 Windows、Linux、macOS 差异

主要产物：

```text
state.json
execution-log.json
quality-gates.json
raw_js_steps
screenshots/
ui_test_execution_report.md
```

---

# 9. 测试失败后续链路

测试执行完成后：

```text
测试报告
→ 可选同步测试结果
→ 失败归因
→ 缺陷聚合
→ 创建 DTS
→ 开发侧定向修复
→ 重新测试
```

## 三类失败

| 类型     | 处理                       |
| -------- | -------------------------- |
| 脚本问题 | 修测试脚本，不创建产品缺陷 |
| 环境问题 | 处理环境，不修改业务代码   |
| 真实 Bug | 生成缺陷并同步 DTS         |

多个相同根因的失败用例合并成一个缺陷，避免重复 DTS。

当前前端 E2E 是**半自动闭环**：

```text
E2E 失败
→ 自动收集证据和分类
→ 用户确认创建缺陷
→ 开发侧 TARGETED_FIX
→ 手动重新触发 E2E
```

后端 UT 和 API 测试已经接入 `/it-dev-workflow` 自动闭环，最多 3 轮。

---

# 10. 外部确定性保障

核心原则：

> LLM 负责理解、生成和分析，程序负责状态、执行、验证、阻断和留证。

## 状态文件

负责：

- 当前阶段
- 当前任务
- 当前批次
- 重试次数
- 环境信息
- 执行结果

## Hooks

负责：

- 探索阶段禁止写代码
- 用户未确认禁止推进
- 产物缺失禁止进入下一阶段
- 定向修复禁止修改白名单外文件
- 超过重试上限强制终止
- 证据不完整禁止生成报告

## Node.js Runner

负责：

- 心跳
- 超时
- 分批执行
- 重试
- 状态落盘
- 环境恢复
- 报告生成

## 编译器和 Playwright

- Maven 退出码判断代码是否真正编译通过
- Playwright 操作真实浏览器
- 不依赖 Agent 自己宣称“执行成功”

## Quality Gates

通过布尔条件决定流程是否允许继续，例如：

```text
设计通过
AND 契约一致
AND 用户确认
→ 允许开发
```

```text
所有用例完成
AND 日志完整
AND 截图完整
→ 允许生成报告
```

一句话：

> LLM 决定建议做什么，Harness 决定是否允许做、是否真的做成、有没有证据。

---

# 11. AGENTS.md 与 Rules 的边界

## AGENTS.md

AGENTS.md 是项目级总则，主要写：

- 项目定位
- Command、Agent、Skill 分层
- 权威数据源
- 全局流程
- 用户确认门
- 全局禁止行为
- 状态恢复
- 上下文隔离
- 异常升级
- 标准产物

它相当于所有 Agent 必须遵守的“项目宪法”。

## Rules

Rules 写具体场景和技术栈策略，例如：

- Java Maven 编译
- Git 安全
- 定向修复
- UI 测试设计
- UI 测试执行
- 缺陷分类
- AUI、Jalor、Starling 规范

Rule 不负责完整流程编排，只约束某个场景应该怎么做。

## 团队级与项目级 Rules

### 团队级

沉淀：

- 技术规范
- 安全底线
- Git、Maven、CI/CD 规则
- 测试和 Review 标准
- 内部框架规范

### 项目级

描述：

- 项目模块
- 目录结构
- 路由模式
- 环境 URL
- settings.xml
- 可修改目录
- 业务约束
- 禁止修改的历史模块

优先级：

```text
企业基线
→ 团队级 Rules
→ 项目级 Rules
→ 当前任务参数
```

项目规则可以细化团队规则，但不能覆盖不可绕过的安全规则。

---

# 12. 客观评价

## 当前优势

1. 需求、设计、任务、测试形成完整契约链。
2. 代码侧具备状态机、断点恢复和定向修复。
3. 前端 E2E 已将大量控制下沉到 Node.js、Playwright 和状态文件。
4. 具备质量门禁、证据链和失败分类。
5. 已经超过普通 Prompt/Skill 工具集，达到流程型 Harness 水平。

## 当前不足

1. 部分硬约束仍然只是 Prompt 约束。
2. 模式判断仍依赖自然语言参数。
3. Markdown 同时承担人类文档和机器状态。
4. 多个状态文件缺少统一状态内核。
5. 缺少完整权限、安全、可观测和评测治理。
6. 前端 E2E 尚未纳入完全自动修复闭环。

## 成熟度判断

```text
L1 Prompt 工具
L2 Agent 工作流
L3 有状态、有门禁、有恢复的 Harness
L4 企业统一治理平台
L5 数据驱动自优化平台
```

当前整体处于：

> **L3，部分前端 E2E 能力接近 L3.5。**

---

# 13. 面试一句话总结

> 这套 OpenCode 研发套件通过 Command、Agent 和 Skill 组织需求、开发和测试流程，再通过状态文件、Hook、Runner、编译器、Playwright MCP、质量门禁和证据链，将 LLM 的概率性行为约束为可恢复、可验证、可审计的企业研发流程。当前已经具备流程型 Harness 能力，下一步重点是建设统一的强类型 Runtime、状态引擎、权限和评测治理。
