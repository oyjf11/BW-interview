# SDD 深度理解(企业 Harness 视角)

> **这篇是什么**:面试官最爱钻 SDD,因为它把需求、评测、验收、防退化串成一条线,顺着任何一头都能追到底。本篇按"一句话 → 三层递进 → 在 harness 里怎么落 → 边界与失败模式 → 讲解稿与追问链"组织,目标是**追到底都不崩**。
> **配套**:《企业harness套件-包装版.md》§3.1、《追问连环题库.md》E 段。

---

## 0. 一句话内核(被问到先说这句)

> **SDD 的内核不是"先写文档",是"评测先行"—— 把'什么算对'在 AI 生成之前,写成一份机器能自动校验的活契约;而这份契约,和评测集里那道题的'标准答案 + 可接受范围 + 陷阱',是同一个东西。**

记住这句的三个关键词:**生成之前**(时机)、**机器能校验**(形式)、**和评测题同一个东西**(本质)。后面所有深挖都从这三点展开。

---

## 1. 第一层:SDD 解决什么 —— 死文档 + AI 放大

传统需求文档有个五十年的老毛病:**它是死的**。写完就和代码分叉,越走越远,三个月后没人信、也没人维护。

AI 时代这件事被**放大**:
- 过去是"人照着过期文档干活";现在多了一个"**模型照着过期文档干活**",而且模型不会停下来质疑"这文档是不是过时了",它只会忠实地把错的实现出来。
- 更隐蔽的:**模型会把需求理解错,然后顺手把测试也按错的理解写了** —— 代码和测试同时误解需求,全绿,但业务是错的。(经典例子:需求"不能删**已发布**的应用"被理解成"不能删**任何**应用",实现和测试一致、全跑绿,业务错了。)

所以需求侧需要一个**不会变成死文档、且判定标准独立于实现者**的东西 —— 这就是 SDD。

〔行业叫法:让文档"活"起来、能被持续执行校验,叫 **Living Documentation / Executable Specification**;判定标准独立于实现者,叫 **Approved Fixtures**(Birgitta)—— 由人预先批准关键输入和预期输出,避免实现 Agent 同时握有"写代码"和"判对错"两个权力。〕

---

## 2. 第二层:核心论断 —— spec 和评测题是同一个东西

这是 SDD 最深、也最值钱的一句。为什么它俩是同一个东西?

因为它俩回答的是**同一个问题**:"什么算做对?"
- 一份合格的 spec,必须说清:正常情况该是什么(标准答案)、哪些算可接受(可接受范围)、哪些是必须挡住的反例(陷阱/边界)。
- 一道合格的评测题,恰好也是:标准答案 + 可接受范围 + 陷阱。

**它们结构同构,所以是同一份制品的两种用法**:
```text
同一份 spec
  ├─ 给开发用:AI 照着它生成代码         (SDD 驱动开发)
  └─ 给平台用:CI 拿它当评测题守门       (评测门禁)
```
**企业里不该建两套** —— 一个团队写 spec 驱动开发,平台拿同一批 spec 当评测题守门。这是 SDD 在企业级最关键的工程红利:**一份制品,三个角色(需求 / 验收标准 / 评测题),一套基础设施。**

〔行业真实出处可引:**Specification by Example**(Gojko Adzic)—— 用具体例子写规格,这些例子同时就是验收测试和活文档,"一份制品同时是规格、测试、文档"正是这本书的核心主张。SDD 是它在 AI Agent 时代的延伸。〕

---

## 3. 第三层:为什么必须"生成之前"+"机器能判"

这一层是认识论,讲透了面试官会觉得你"真想明白了"。

### 3.1 为什么是"生成之前"而不是"事后验收"
> **你没法在事后证明一个 AI 输出是对的 —— 因为 AI 的输出空间是开放的,你不可能枚举所有"它可能怎么错"。你只能在生成之前,把"什么算对"定死。**

事后验收是"我看看它对不对",这是被动的、不可穷尽的;事前定义是"我先规定对的边界,再让它往里生成",这是主动的、可判定的。SDD 的"先"不是流程顺序的先,是**认识论上的先** —— 对错的定义必须先于、且独立于生成过程。

### 3.2 为什么必须"机器能判"
口头/自然语言的 spec 仍然是软的,会漂、会被各自解读。只有把 spec 落成**机器能自动校验的形式**,它才能:
- 接进 CI,**代码一改自动判一遍,不符合当场红 → 这是它不退化的根本机制**;
- 客观部分(类型、契约、断言、覆盖集合)直接代码判,不消耗人和模型。

〔行业叫法:把规范从"wiki 上等人遵守"变成"流水线里过不去的硬规则",叫 **Architecture Fitness Function**(Thoughtworks)—— 架构/契约特征要可执行、可持续验证。SDD 让需求本身也变成一种 Fitness Function。〕

### 3.3 但不是所有"对"都能机器判 —— 衔接 Computational/Inferential
诚实的边界:spec 能把**客观内核**(契约一致、字段类型、必测场景覆盖、断言非空壳)写成机器可判;但**主观残留**(这个需求理解得对不对、方案合不合理)消不掉,这部分仍要上 LLM 裁判 + gold set。
> **SDD 不是消灭主观判断,是把能确定性判的尽量前移、固化成机器可判,把昂贵的人类/模型判断压缩到真正消不掉的那一小点上。**

---

## 4. SDD 在这套 Harness 里具体怎么落:契约链

SDD 在我们这里不是抽象理念,是一条**贯穿全生命周期的契约链**。spec 是**单一事实源(Single Source of Truth)**,被线程式地穿过每一个阶段:

```text
            ┌─────────────── 单一事实源(spec / 契约) ───────────────┐
需求站      requirement-analysis.md
  │         design_document.md
  │         design_api.yaml          ← API 契约(contract-first)
  │         design_database.sql      ← 数据模型契约
  ▼
开发站      tasks.md  ←── 三方覆盖校验 ──┐
  │           设计功能点 ↔ tasks 功能点  │  (spec 驱动任务拆分,
  │           代码文件清单 ↔ tasks 文件  │   漏了自动补,≤3 轮)
  │           API paths ↔ tasks 接口    ┘
  │         代码 ←── 照 spec 生成
  ▼
测试站      测试用例 ←── 是设计的"派生品"
  │           "测试验证的是设计与实现的一致性,不重新分析需求"
  │           API 测试直接来自 design_api.yaml
  ▼
验收门禁    设计通过 AND 契约三方一致 AND 用户确认 → 才许进开发
            (Quality Gate 用布尔条件卡)
```

**四个落地要点,每个都能单独被追问**:

1. **契约前置(contract-first)**:`design_api.yaml` 是 API 契约,开发和测试都从它派生,而不是各写各的。这就是 OpenAPI 式契约先行在 AI 流程里的应用。
2. **三方一致性校验**:需求、设计、API 契约必须对得上 —— 这是把"spec 之间不打架"做成了机器校验(集合比对),不是靠人看。
3. **测试是 spec 的派生品,不是重新分析需求**:这是 SDD 在测试侧最重要的纪律 —— 测试不"二次理解需求"(那会引入第二次误解),而是机械地验证"实现 vs 设计是否一致"。
4. **门禁卡在移交点**:`契约一致 AND 用户确认` 才许开发 —— spec 没闭合,流程根本走不下去。

---

## 5. 边界与失败模式(💀 主动讲,显出你真想过)

讲 SDD 只讲好处会显得浅。**主动抛风险**才显资深:

| 失败模式 | 为什么发生 | 怎么治 |
|---------|-----------|--------|
| **spec 本身就错了** | 人/AI 一开始就误解需求,spec 和实现一起错,全绿但业务错 | 判定标准独立于实现者(Approved Fixtures);关键 spec 由人预先批准;复杂需求强制用户确认门 |
| **过度规约(锁太死)** | spec 把实现细节也钉死,AI 没有发挥空间,改个内部实现就误红 | spec 只约束"对外行为/契约/必测场景",不约束内部实现;锁的是断言的期望值,不是怎么写 |
| **欠规约(留太松)** | spec 含糊,"什么算对"没说清,等于没 spec | 用"具体例子"写(Specification by Example),含糊处补反例/陷阱题 |
| **spec 又变回死文档** | 写完没接 CI,没人逼它和代码对齐 | 必须接进 CI 当门禁,代码一改自动判 —— 这是 SDD 和"写需求文档"的根本分界 |
| **spec 维护没人负责** | 企业里题库/spec 会过期、失效 | 它是组织产物:设"题库负责人"常设岗,每抓到一次漏判补一条 |

---

## 6. 和相邻概念划清界限(⚠️ 高频追问)

| 对比对象 | SDD 的区别 |
|---------|-----------|
| **写需求文档** | 文档是死的、给人读、不校验;spec 是活的、机器判、不符合当场红 |
| **TDD(测试驱动)** | TDD 是代码层"先写测试";SDD 是更上游"先定义什么算对",且同一份 spec 同时服务开发驱动 + 评测守门 + 验收,一份顶三份 |
| **BDD / Gherkin** | BDD 是 SDD 的一种表达形式(用 Given-When-Then 写可执行场景);SDD 是更宽的方法论,不限表达形式,核心在"评测先行 + 一份制品多用" |
| **契约测试(Pact 等)** | 契约测试管"服务间接口不打架";SDD 的契约链覆盖需求→设计→API→测试全程,API 契约只是其中一环 |
| **普通 Prompt 工程** | Prompt 是软约束、给一次生成用;spec 是硬契约、跨整个生命周期、机器可判 |

**一句收口**:别人理解的 SDD 常停在"先写规格再开发";我理解的 SDD 是"**把对错的定义前移、固化成机器可判的单一事实源,让它同时驱动开发、守门评测、抵抗退化**"。

---

## 7. 一个具体例子(被要求"举个例子"时用)

需求:"用户登录,支持账号密码;**连续错 3 次锁定 15 分钟**。"

- **传统做法**:写句需求文档 → AI 实现 → 回头补测试。风险:AI 可能把"错 3 次"实现成"错 3 次后立即锁定但没设解锁时间",测试也按同样理解写,全绿,但少了"15 分钟自动解锁"。
- **SDD 做法**:生成之前先写 spec(= 评测题):
  ```text
  标准答案:第 3 次错误后账户锁定;15 分钟后自动解锁可再登。
  可接受范围:锁定提示文案不限;计时精度 ±数秒可接受。
  陷阱/反例:① 错 2 次不应锁(边界);② 锁定期内输对密码也不应放行(易漏);
            ③ 15 分钟后必须自动解锁(最易被实现和测试一起漏掉)。
  契约:design_api.yaml 中 /login 返回码含 423 Locked + retryAfter 字段。
  ```
- **效果**:这份 spec 同时是 ① AI 生成的依据、② tasks 拆分的覆盖校验源、③ API/UI 测试的派生源、④ CI 门禁的评测题。陷阱 ③ 一旦被漏实现,CI 当场红 —— 不会"全绿但业务错"。

---

## 8. 讲解稿

### 1 分钟版(被问"讲讲你对 SDD 的理解")
> SDD 字面是"规格驱动开发",但它的内核是**评测先行** —— 把"什么算对"在 AI 生成之前,写成一份**机器能自动校验的活契约**。最关键的一点:这份 spec 和评测集里那道题的"标准答案+可接受范围+陷阱"**是同一个东西**,所以企业里不建两套 —— 团队拿它驱动开发,平台拿同一批 spec 当评测门禁。它解决传统需求文档"是死的、写完没人维护"这个五十年老毛病,AI 时代更要命因为模型会照着过期文档忠实地把错的实现出来。在我们 harness 里它落成一条契约链:需求→设计→API→测试,API 契约前置、三方一致性机器校验、测试是设计的派生品而不是二次理解需求、门禁卡在移交点。

### 3 分钟版(展开)
按 §1→§2→§3.1→§4→§5 的顺序讲:先讲死文档+AI放大的问题,再亮"spec=评测题"这个核心论断,再讲"为什么必须生成之前"(你没法事后证明AI对),再落到契约链怎么穿全程,最后主动抛一个失败模式(spec本身错了→Approved Fixtures)收尾,显出边界感。

---

## 9. SDD 专属追问链(刷这个,接得住连环追)

**Q. SDD 和 TDD 不就一回事?** → §6:更上游、一份制品多用、机器可判的单一事实源。
- **追问:那和 BDD/Gherkin 呢?** → BDD 是 SDD 的一种表达形式,SDD 不限形式,核心在评测先行+一份多用。

**Q. spec 自己会不会也过期变死文档?** → §5 + §3.2:接进 CI 当门禁,代码一改自动判,不符合当场红。死文档的根因是"没人逼它和代码对齐",spec 用机器逼。

**Q. 写 spec 不也是成本?不就是把写文档的负担平移了?** → §2:不是平移是合并 —— 一份 spec 同时当开发驱动+评测题+验收标准,一份顶三份;省的是反复对齐需求和实现的隐性成本。

**Q. 凭什么说 spec 和评测题是同一个东西?** → §2:都回答"什么算对",结构同构(标准答案+可接受范围+陷阱),所以是一份制品两种用法。引 Specification by Example。

**Q. 为什么强调"生成之前"?事后测不行吗?** → §3.1:AI 输出空间开放,事后不可穷尽证明它对;只能事前定义对的边界再往里生成。认识论上的"先"。

**Q. ⚠️ 如果 spec 本身写错了呢?那全链路一起错。** → §5 第一行:这是最该承认的风险。治法是判定标准独立于实现者(Approved Fixtures)+ 关键 spec 人工预批准 + 复杂需求强制用户确认门。**实现者不能同时是出题人。**

**Q. 不是所有需求都能写成机器能判的 spec 吧?** → §3.3:对,所以分客观内核(机器判)+ 主观残留(上 LLM 裁判+gold set)。SDD 不消灭主观,是把它压到最小并隔离。

**Q. 💀 SDD 听起来很理想,落地阻力在哪?** → §5 末行 + 组织:最大阻力不是技术是"谁来持续维护 spec/题库" —— 它是组织产物,要设常设岗位,这也是 demo 永远到不了企业级的原因。

---

## 10. 三个最容易被记住的金句(挑一句收尾)

1. **"SDD 的'先'不是流程的先,是认识论的先 —— 对错的定义必须先于、且独立于生成。"**
2. **"spec 和评测题是同一个东西 —— 一份制品,三个角色(需求/验收/评测题),一套基础设施。"**
3. **"传统需求文档是死的,SDD 把它做成了机器逼着和代码对齐的活契约 —— 它不退化,是因为代码一改它当场红。"**
