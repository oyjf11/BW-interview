# SDD 深度理解(企业 Harness 视角)

> **这篇是什么**:面试官最爱钻 SDD,因为它把需求、评测、验收、防退化串成一条线,顺着任何一头都能追到底。本篇按"一句话 → 三层递进 → 在 harness 里怎么落 → 边界与失败模式 → 讲解稿与追问链"组织,目标是**追到底都不崩**。
> **配套**:《企业harness套件-包装版.md》§3.1、《追问连环题库.md》E 段。

---

## 0. 一句话内核(被问到先说这句)

> **SDD(Spec-Driven Development,规格驱动开发)的核心,是把"要做什么"在写代码之前固化成一份持久的、可反复使用的规格(spec),让它成为单一事实源(single source of truth),驱动 AI 生成代码并持续校验 —— 用业界原话讲:"intent is the source of truth(意图即事实源),代码服务于规格,而不是规格服务于代码"。**

三个关键词:**规格先于代码**(时机)、**单一事实源**(地位)、**驱动生成 + 持续校验**(作用)。

〔出处:这是 GitHub Spec Kit 对 SDD 的官方定义 —— "Spec-Driven Development inverts the traditional power structure: specifications do not serve code, code serves specifications." AWS Kiro、OpenSpec 等主流工具的定位与此一致。〕

再补一层(也是行业标准、不是个人发明):一份写得好的 spec **本身就能当验收测试用**。这是 Gojko Adzic《Specification by Example》的核心主张 —— 同一份"带具体例子的规格",同时是规格、验收测试、活文档。SDD 可以看作这个思路在 AI Agent 时代的落地;Adzic 本人也公开评论过 GitHub Spec Kit,认为它延续了 BDD 的"意图先行",但提醒别把流程做得太僵。

---

## 1. 第一层:SDD 解决什么 —— "vibe coding"不可靠 + 死文档

业界推 SDD 的直接动因,是所谓 **"vibe coding"(凭感觉编程)的不可靠**:你给 AI 描述个目标,它返回一段"看着对、但跑不对"的代码。GitHub 对此的判断是 —— 问题不在模型能力,而在用法:"我们把编码 Agent 当搜索引擎用,其实该把它当一个字面化的结对程序员",它需要的是无歧义的规格,而不是一句模糊的需求。

往深一层,是需求文档五十年的老毛病:**文档是死的**。写完就和代码分叉,越走越远,三个月后没人信、也没人维护。AI 时代这件事被**放大**:
- 过去是"人照着过期文档干活";现在多了一个"**模型照着过期文档干活**",而且模型不会停下来质疑"这文档是不是过时了",它只会忠实地把错的实现出来。
- 更隐蔽的:**模型会把需求理解错,然后顺手把测试也按错的理解写了** —— 代码和测试同时误解需求,全绿,但业务是错的。(经典例子:需求"不能删**已发布**的应用"被理解成"不能删**任何**应用",实现和测试一致、全跑绿,业务错了。)

所以需求侧需要一个**不会变成死文档、且判定标准独立于实现者**的东西 —— 这就是 SDD 要解决的。

〔行业术语:让文档"活"起来、能被持续执行校验,叫 **Living Documentation / Executable Specification**(Cyrille Martraire);判定标准独立于实现者、由预先批准的期望输出兜底,叫 **Approval Testing / Golden Master**(Llewellyn Falco、Emily Bache)—— 避免实现 Agent 同时握有"写代码"和"判对错"两个权力。〕

---

## 2. 第二层:核心论断 —— 规格即验收,一份制品多用

SDD 最有价值的一点:**一份写得好的 spec,本身就是验收标准。** 这不是个人发明,是 Gojko Adzic《Specification by Example》的核心主张 —— 用具体例子写规格,这些例子同时就是验收测试,也是活文档。

为什么 spec 和验收测试是同一个东西?因为它俩回答**同一个问题**:"什么算做对?"
- 一份合格的 spec,必须说清:正常情况该是什么(期望行为)、哪些算可接受(可接受范围)、哪些是必须挡住的反例(边界 / 反例)。
- 一条合格的验收测试,恰好也是:期望行为 + 可接受范围 + 反例。

它们结构同构,所以**是同一份制品的两种用法**:
```text
同一份 spec
  ├─ 给开发用:AI 照着它生成代码      (驱动开发 —— Spec Kit / Kiro / OpenSpec 都这么干)
  └─ 给验收用:这些例子就是验收测试    (Specification by Example / ATDD)
```
**企业里不该建两套** —— 同一份 spec 既驱动开发,又当验收标准。这是 SDD 在工程上的核心红利:**一份制品,多个角色(需求 / 设计依据 / 验收标准),一套基础设施。**

〔行业出处:**Specification by Example**(Gojko Adzic)、**ATDD**(验收测试驱动开发)—— "一份制品同时是规格、测试、活文档"正是这一脉的主张。〕

> **【这是我的延伸,和上面的行业标准分开讲】** 我在我们项目里想更进一步:把"同一份 spec"也接到**评测门禁 / LLM 评测集(gold set)**上 —— 让 spec 不只校验"实现 vs 设计一致",还守住"模型生成质量"。**这一层是我的工程主张,不是 Specification by Example 的原意,而且在我们 harness 里目前是 roadmap、还没建(见 §4 末)。** 面试里我会明确把它和行业标准区分开,不混为一谈。

---

## 3. 第三层:为什么强调"生成之前"+"机器能判"

### 3.1 为什么是"生成之前",而不是"事后验收"
> **AI 的输出空间是开放的,你没法在事后枚举它所有可能的错法、从而证明它"对"。可行的做法是反过来:生成之前先把"什么算对"定下来,再让它往这个边界里生成。**

这跟传统工程里的 **contract-first(契约先行)** 和 **测试先行(TDD / ATDD)** 是同一个思路 —— 先定义可判定的边界,再实现。SDD 的"先"不是流程排序上的先,是**逻辑上的先**:对错的定义要先于、且独立于生成过程,否则就成了"实现者自己给自己判卷"。

### 3.2 为什么要"机器能判"
口头 / 自然语言的 spec 是软的,会漂移、会被各自解读。只有把 spec 落成**机器能自动校验的形式**,它才能:
- 接进 CI,**代码一改自动判一遍,不符合当场红 → 这是 spec 不退化成死文档的根本机制**;
- 客观部分(类型、契约、断言、覆盖集合)直接由代码判,不消耗人和模型。

让自然语言需求"机器可解析"的一个真实范例是 **EARS(Easy Approach to Requirements Syntax)** —— 用 "WHEN〔条件〕THE SYSTEM SHALL〔行为〕" 这种受限句式写需求。AWS Kiro 就用 EARS 写 `requirements.md`,并据此自动生成 property-based 测试。

〔行业叫法:把规范从"wiki 上等人遵守"变成"流水线里过不去的硬规则",叫 **Architecture Fitness Function**(出自《Building Evolutionary Architectures》,Ford / Parsons / Kua)—— 架构 / 契约特征要可执行、可持续验证。〕

### 3.3 但不是所有"对"都能机器判
诚实的边界:spec 能把**客观内核**(契约一致、字段类型、必测场景覆盖、断言非空壳)写成机器可判;但**主观残留**(需求理解得对不对、方案合不合理)消不掉。
> **SDD 不消灭主观判断,而是把能确定性判的尽量前移、固化成机器可判,把昂贵的人 / 模型判断压缩到真正消不掉的那一小点上。**

那一小点要靠 **LLM 裁判 + gold set(评测集)** 来兜 —— 这属于"评测治理",在我们 harness 里是 **L4 roadmap、当前未建**(见 §4 末、§12 成熟度判断)。

---

## 4. SDD 在这套 Harness 里具体怎么落:契约链

SDD 在我们这里不是抽象理念,是一条**贯穿全生命周期的契约链**。spec 是**单一事实源(Single Source of Truth)**,被线程式地穿过每一个阶段:

```text
            ┌─────────────── 单一事实源(spec / 契约) ───────────────┐
需求站      requirement-analysis.md
  │         design_document.md
  │         design_api.yaml          ← API 契约(contract-first)
  │         design_database.sql      ← 数据模型契约
  ▼
开发站      tasks.md  ←── 三方覆盖校验 ──┐
  │           设计功能点 ↔ tasks 功能点  │  (spec 驱动任务拆分,
  │           代码文件清单 ↔ tasks 文件  │   漏了自动补,≤3 轮)
  │           API paths ↔ tasks 接口    ┘
  │         代码 ←── 照 spec 生成
  ▼
测试站      测试用例 ←── 是设计的"派生品"
  │           "测试验证的是设计与实现的一致性,不重新分析需求"
  │           API 测试直接来自 design_api.yaml
  ▼
验收门禁    设计通过 AND 契约三方一致 AND 用户确认 → 才许进开发
            (Quality Gate 用布尔条件卡)
```

**四个落地要点,每个都能单独被追问**:

1. **契约前置(contract-first)**:`design_api.yaml` 是 API 契约,开发和测试都从它派生,而不是各写各的。这就是 OpenAPI 式契约先行在 AI 流程里的应用。
2. **三方一致性校验**:需求、设计、API 契约必须对得上 —— 这是把"spec 之间不打架"做成了机器校验(集合比对),不是靠人看。
3. **测试是 spec 的派生品,不是重新分析需求**:这是 SDD 在测试侧最重要的纪律 —— 测试不"二次理解需求"(那会引入第二次误解),而是机械地验证"实现 vs 设计是否一致"。
4. **门禁卡在移交点**:`契约一致 AND 用户确认` 才许开发 —— spec 没闭合,流程根本走不下去。

**和业界工具的对应**(被追问"凭什么说同构"时,逐行说出对应关系,别只甩名词):

| AWS Kiro | 我们的 harness | 谁更强 |
|---|---|---|
| `requirements.md`(EARS 语法) | `requirement-analysis.md` | 持平(我们暂未上 EARS) |
| `design.md` | `design_document.md` + **`design_api.yaml`** | **我们多一层 API 契约(contract-first),测试直接派生** |
| `tasks.md`(每任务回溯需求) | `tasks.md`(三方覆盖校验) | **我们把一致性做成确定性集合比对,漏了自动补;Kiro 靠 Agent 自觉** |
| 按 EARS 自动生成 property-based 测试 | —— | **Kiro 更强,我们还没做到** |

> 主动说"我比它强在哪、弱在哪",比只说"同构"可信得多。

> 〔**L4 roadmap、当前未建**〕把同一份 spec 进一步接入**评测门禁 / 评测治理**(LLM 裁判 + gold set 守模型生成质量)。我们 harness 当前是 L3(见《开发套件全流程.md》§12),这一项尚未落地。面试中我会把"已建的契约链"和"未建的评测治理"分清楚,不把 roadmap 说成已有。

---

## 5. 边界与失败模式(主动讲)

只讲好处会显得浅。把风险和治法一并讲清:

| 失败模式 | 为什么发生 | 怎么治 |
|---------|-----------|--------|
| **spec 本身就错了** | 人/AI 一开始就误解需求,spec 和实现一起错,全绿但业务错 | 判定基准独立于实现者(**Approval Testing**:期望输出由人预先批准);关键 spec 人工预批;复杂需求强制用户确认门 |
| **过度规约(锁太死)** | spec 把实现细节也钉死,AI 没有发挥空间,改个内部实现就误红 | spec 只约束"对外行为/契约/必测场景",不约束内部实现;锁的是断言的期望值,不是怎么写 |
| **欠规约(留太松)** | spec 含糊,"什么算对"没说清,等于没 spec | 用"具体例子"写(Specification by Example),含糊处补反例/陷阱题 |
| **spec 又变回死文档** | 写完没接 CI,没人逼它和代码对齐 | 必须接进 CI 当门禁,代码一改自动判 —— 这是 SDD 和"写需求文档"的根本分界 |
| **spec 维护没人负责** | 企业里题库/spec 会过期、失效 | 它是组织产物:设"题库负责人"常设岗,每抓到一次漏判补一条 |

---

## 6. 和相邻概念划清界限(⚠️ 高频追问)

| 对比对象 | SDD 的区别 |
|---------|-----------|
| **写需求文档** | 文档是死的、给人读、不校验;spec 是活的、机器判、不符合当场红 |
| **TDD(测试驱动)** | TDD 是代码层"先写测试";SDD 是更上游"先定义什么算对",且同一份 spec 同时服务开发驱动 + 评测守门 + 验收,一份顶三份 |
| **BDD / Gherkin** | BDD 是 SDD 的一种表达形式(用 Given-When-Then 写可执行场景);SDD 是更宽的范式,不限表达形式,核心在"规格先行 + 一份制品多用" |
| **契约测试(Pact 等)** | 契约测试管"服务间接口不打架";SDD 的契约链覆盖需求→设计→API→测试全程,API 契约只是其中一环 |
| **普通 Prompt 工程** | Prompt 是软约束、给一次生成用;spec 是硬契约、跨整个生命周期、机器可判 |

**和当下 SDD 工具划界**(行业全景,面试官常拿来当锚点):

| 工具 | 形态 | 侧重 | 代价 / 局限(被问缺点时用) |
|---|---|---|---|
| **GitHub Spec Kit** | 开源脚手架(CLI + 模板) | Spec→Plan→Tasks→Implement;`constitution.md` 定项目铁律;工具无关,支持 30+ Agent | 重、流程多;Gojko Adzic 批评它"可能把敏捷想甩掉的僵化又带回来";小改动/小 bug 用它是 overkill |
| **AWS Kiro** | Agentic IDE(基于 Code OSS) | `requirements/design/tasks` 三件套;requirements 用 **EARS** 语法;按 EARS 自动生成 property-based 测试 | 要学 EARS 受限句式,有学习曲线;对探索性 / "vibe" 编码很别扭 |
| **OpenSpec**(Fission-AI) | 轻量开源 CLI | **Propose→Apply→Archive**;**delta spec**(标 ADDED/MODIFIED/REMOVED);brownfield(存量代码)优先 | 轻量、约束弱;靠 Agent 自觉遵守,缺确定性门禁 |

**一句收口(我的理解)**:行业里 SDD 多停在"规格驱动生成";我们项目把它往工程纵深做了一步 —— **把对错的定义前移、固化成机器可判的单一事实源,用确定性程序(Hook/Runner/门禁)逼着它和代码对齐、抵抗退化**。("同时守门评测"是我的 roadmap,不是已建,见 §4 末。)

---

## 7. 一个具体例子(被要求"举个例子"时用)

需求:"用户登录,支持账号密码;**连续错 3 次锁定 15 分钟**。"

- **传统做法**:写句需求文档 → AI 实现 → 回头补测试。风险:AI 可能把"错 3 次"实现成"错 3 次后立即锁定但没设解锁时间",测试也按同样理解写,全绿,但少了"15 分钟自动解锁"。
- **SDD 做法**:生成之前先写 spec(= 验收标准)。需求用 **EARS** 受限句式写,天然可判:
  ```text
  R1  WHEN 同一账号连续 3 次密码错误 THE SYSTEM SHALL 锁定该账号
  R2  WHEN 账号处于锁定态且距锁定未满 15 分钟 THE SYSTEM SHALL 拒绝登录(即使密码正确)
  R3  WHEN 账号锁定满 15 分钟 THE SYSTEM SHALL 自动解锁,允许再次登录

  可接受范围:锁定提示文案不限;计时精度 ±数秒可接受。
  反例/边界:① 错 2 次不应锁(R1 边界);② 锁定期内输对密码也不放行(R2,易漏);
            ③ 15 分钟后必须自动解锁(R3,最易被实现和测试一起漏掉)。
  契约:design_api.yaml 中 /login 返回码含 423 Locked + retryAfter 字段。
  ```
- **效果**:这份 spec 同时是 ① AI 生成的依据、② tasks 拆分的覆盖校验源、③ API/UI 测试的派生源 —— 这三用途在我们 harness 里都已落地。反例 ③ 一旦被漏实现,测试当场红,不会"全绿但业务错"。
  〔**roadmap**:把这份 spec 再接入评测门禁 / gold set,守"模型生成质量"——当前未建。〕

---

## 8. 讲解稿

### 1 分钟版(被问"讲讲你对 SDD 的理解")
> SDD(规格驱动开发)的核心,是把"要做什么"在写代码之前固化成一份持久的规格,让它当单一事实源、驱动 AI 生成并持续校验 —— 用 GitHub Spec Kit 的话讲就是"intent is the source of truth,代码服务于规格"。业界现在落地的工具有 GitHub Spec Kit、AWS Kiro、OpenSpec,共同点都是"规格先行、机器可校验"。它解决的是传统需求文档"是死的、写完没人维护"这个老毛病 —— AI 时代更要命,因为模型会照着过期或模糊的需求,忠实地把错的实现出来,还顺手把测试也按错的理解写了,全绿但业务错。在我们 harness 里,它落成一条契约链:需求→设计→API→测试,API 契约前置、三方一致性机器校验、测试是设计的派生品而不是二次理解需求、门禁卡在移交点。再往前是评测治理(用 spec 守模型生成质量),那是我们的 roadmap。

### 3 分钟版(展开)
按 §1→§2→§3→§4→§5 的顺序讲:先讲"vibe coding 不可靠 + 死文档"这个行业动因(引 Spec Kit 那句"把 Agent 当字面化结对程序员"),再讲"规格即验收、一份制品多用"并挂 Specification by Example 出处,再讲"为什么强调生成之前"(AI 输出空间开放、事后不可穷尽,所以契约先行),再落到我们真实的契约链怎么穿全程,最后主动抛一个失败模式(spec 本身错了→Approval Testing、判定独立于实现者)收尾。全程把"行业标准"和"我的延伸/roadmap"分开说,显出边界感。

---

## 9. SDD 专属追问链(刷这个,接得住连环追)

**Q. SDD 具体是什么?业界有哪些落地?** → §0 + §6:规格作为单一事实源驱动 AI 生成并持续校验("intent is the source of truth")。落地工具:GitHub Spec Kit(Spec→Plan→Tasks)、AWS Kiro(requirements/design/tasks + EARS)、OpenSpec(Propose→Apply→Archive + delta spec)。

**Q. 你用过 Spec Kit / Kiro / OpenSpec 吗?** → 诚实话术:了解三者的设计理念和差异(脚手架 / IDE / 轻量 CLI),我们项目没直接用它们,而是在 OpenCode 上自建了一套同构的契约链(需求→设计→API→测试),并把一致性校验、状态机、质量门禁做成了确定性程序。要点是讲清"我懂这套范式、且自己实现过",而不是声称用过某个具体产品。

**Q. SDD 和 TDD 不就一回事?** → §6:SDD 更上游(先定义"做什么"),一份制品多用;TDD 是代码层"先写测试"。
- **追问:那和 BDD / Gherkin 呢?** → BDD 是表达形式(Given-When-Then);SDD 是更宽的范式,不限形式。Gojko Adzic 也指出 Spec Kit 延续了 BDD 的"意图先行"。

**Q. EARS 是什么?** → §3.2:Easy Approach to Requirements Syntax,用 "WHEN〔条件〕THE SYSTEM SHALL〔行为〕" 受限句式写需求,让自然语言需求可解析、可测试;Kiro 用它生成 property-based 测试。

**Q. spec 自己会不会也过期变死文档?** → §5 + §3.2:接进 CI 当门禁,代码一改自动判,不符合当场红。死文档的根因是"没人逼它和代码对齐",spec 用机器逼。

**Q. 写 spec 不也是成本?不就是把写文档的负担平移了?** → §2:不是平移是合并 —— 一份 spec 同时当开发驱动 + 验收标准,省的是反复对齐需求和实现的隐性成本(Specification by Example 的核心收益)。

**Q. 凭什么说 spec 和验收测试是同一个东西?** → §2:都回答"什么算对",结构同构(期望行为 + 可接受范围 + 反例),所以是一份制品两种用法。引 Specification by Example / ATDD。

**Q. 为什么强调"生成之前"?事后测不行吗?** → §3.1:AI 输出空间开放,事后不可穷尽证明它对;只能事前定义对的边界再往里生成。这跟 contract-first / 测试先行同源。

**Q. 如果 spec 本身写错了呢?那全链路一起错。** → §5 第一行:这是最该承认的风险。治法是判定基准独立于实现者(**Approval Testing**)+ 关键 spec 人工预批 + 复杂需求强制用户确认门。**实现者不能同时是出题人。**

**Q. "测试是设计的派生品"——那要是设计本身漏了一个场景呢?测试不就跟着一起漏?** → §5 第一行:对,测试不二次分析需求,是为了不引入**第二次**误解;但它救不了**第一次**误解(设计漏场景)。这一层只能靠判定基准独立于实现者 —— 关键 spec 人工预批(Approval Testing)+ 复杂需求强制用户确认门。**实现者不能同时是出题人**,这是唯一兜底。不假装测试派生能解决"需求本身错"。

**Q. 不是所有需求都能写成机器能判的 spec 吧?** → §3.3:对,所以分客观内核(机器判)+ 主观残留(上 LLM 裁判 + gold set)。SDD 不消灭主观,是把它压到最小并隔离。

**Q. 你说的"用 spec 当评测门禁"落地了吗?** → 诚实回答:没有,那是 roadmap。我们 harness 当前 L3,已建的是契约链 + 质量门禁 + 证据链;评测治理(LLM 裁判 + gold set)是下一步(L4)。不把计划说成已有。

**Q. SDD 落地阻力在哪?** → §5 末行 + 组织:最大阻力不是技术,是"谁来持续维护 spec" —— 它是组织产物,需要常设维护责任,这也是 demo 难到企业级的原因。

---

## 10. 三句话收口(挑一句结尾)

1. **"SDD 的'先'不是流程排序的先,是逻辑上的先 —— 对错的定义要先于、且独立于生成,否则就是实现者自己给自己判卷。"**
2. **"一份写得好的 spec 本身就是验收标准(Specification by Example)—— 一份制品,既驱动开发,又当验收,一套基础设施。"**
3. **"传统需求文档是死的;SDD 把它接进 CI、用确定性程序逼着和代码对齐 —— 它不退化,是因为代码一改它当场红。"**
