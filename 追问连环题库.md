# 追问连环题库(模拟面试官层层深挖)

> **用法**:面试官很少问一个就停,会顺着你的答案往下钻 2–3 层。本库按主题做成"追问链",每问配**一句答题锚点**。刷的时候盖住锚点自己先答,答不出的就是漏洞。
> **配套**:《企业harness套件-包装版.md》《面试讲解稿与追问预案.md》。
> **标记**:🔥=高频必问 / ⚠️=陷阱题,答不好会露馅 / 💀=反方向质疑,考你有没有真想过。

---

## A. 架构与设计取舍

**A1 🔥** 为什么要分 Command / Agent / Skill 三层?合成一层不行吗?
> 关注点分离:Command 管入口路由(变化频繁)、Agent 管编排状态(需要上下文)、Skill 管领域能力(可复用)。合一层就是上帝对象,改一处炸全局。
- **追问 A1.1** 那 Skill 和 Agent 边界怎么定?什么时候该新建 Agent 而不是 Skill?
  > Skill 是无状态能力(给输入出输出);需要跨多步维护状态、做动态判断、编排多个 Skill 时才上 Agent。边界测试:能不能"用完即走"。
- **追问 A1.2 💀** 90 个 Skill 是不是太多了?会不会是过度设计?
  > 承认风险,这正是要做"熵管理"的原因 —— 定期清理低使用率 Skill。数量不是问题,缺治理才是;我下一步要加使用率统计和废弃机制。

**A2 🔥** 控制面/数据面分离,具体哪些东西放控制面?边界怎么画?
> 控制面:打分、限流、权限、审批、留证、模型网关 —— 凡是"不碰业务、所有业务线共用"的。数据面:具体怎么处理需求/写代码/跑测试,允许各线不同。判据:加一条新安全检查时,要不要改业务 Agent?要改就说明边界画错了。
- **追问 A2.1 ⚠️** 控制面会不会成为单点故障 / 性能瓶颈?
  > 会,所以控制面要无状态可水平扩展 + 校验异步化 + 降级策略(控制面挂了走只读/拒绝写,而不是放行)。安全默认拒绝,不默认放行。
- **追问 A2.2 💀** 业务团队嫌控制面卡他们,要绕过怎么办?
  > 技术上绕不过(校验不在他们手里);组织上靠分层门禁不一刀切(本地快检几分钟)+ Sensor 输出"错在哪+怎么改"降低摩擦。绕过需求往往是门禁设计太粗,该细化而不是放开。

**A3** 三种编排模式(直接委派/Agent编排/自包含流程)怎么选?
> 按"任务边界是否明确 + 是否需要状态恢复"。短平快直接委派;要动态判断上 Agent;长流程要断点恢复用自包含流程。

---

## B. 确定性与护栏(Guardrails)

**B1 🔥** "约束在执行前生效"具体怎么做到?举个不靠提示词的例子。
> 提交前确定性脚本校验(过不了提交不了)、探索阶段 Hook 禁用 Write/Edit、定向修复白名单外文件物理拦截。例子:防删断言的 swc AST 校验。
- **追问 B1.1 🔥** swc 那个校验展开讲,为什么前两版不行?
  > 文本匹配被注释/字符串里关键字误判;手写解析器不健全(被"解析得了 Vue 吗"问破);swc 解析成语法树按节点角色判 —— 定位语句可改,流进断言节点的期望值/匹配方式就锁死。
- **追问 B1.2 ⚠️** AST 校验本身有 bug 放过了作弊怎么办?谁来测这个校验?
  > 校验自己也是被测对象:作弊未遂做成陷阱题永久入库,校验改动要先过这批陷阱题。这就是 Steering Loop —— 修的是系统条件,不是单个 case。
- **追问 B1.3 💀** 你怎么保证你没把"正常的合理修改"也误锁了?(误杀)
  > 节点角色区分:只锁断言的"期望值和匹配方式",定位/选择器/等待逻辑都放行。误杀率靠陷阱题库+真实回归集双向验证。

**B2** Guides 和 Sensors 为什么两个都要?只要其中一个会怎样?
> 只有 Guides 不知道做到没;只有 Sensors 不停犯错不停返工。前馈+反馈才闭环。
- **追问 B2.1** 你说"最好的 Guide 是程序结构不是文字",为什么?
  > 强类型接口/固定目录/只能正确调用的 SDK,比"请你注意 X"更难被忽略 —— 文字是软约束,结构是硬约束。
- **追问 B2.2** "好的 Sensor 要输出错在哪+怎么改",为什么不只说"错了"?
  > 因为这段报错就是 Agent 下一轮的修复上下文。只说"错了"等于让它重猜,带上"正确做法"才能收敛。

**B3 💀** 你这么多确定性校验,是不是把 AI 的灵活性给捆死了?那还要 AI 干嘛?
> 分工:LLM 负责理解/生成/分析(发挥灵活性的地方),程序负责验证/阻断/留证(需要确定性的地方)。我捆的不是"它怎么想",是"它能不能在没证据的情况下说做成了"。灵活性留在生成,确定性放在验收。

---

## C. 评测体系(最难、最区分水平)

**C1 🔥** "客观内核 + 主观残留"怎么拆?给一段具体拆给我看。
> 以自愈为例:客观内核=沙箱重跑过没过(代码判)+断言没删空(AST)+选择器唯一(代码判);主观残留=修复语义合不合理(才上模型)。原则:能确定性判的绝不上 LLM。
- **追问 C1.1 ⚠️** "选择器唯一"也能代码判,但"定位对了没"呢?重跑过了不就对了?
  > 重跑过 ≠ 语义对。真实案例:改版后页面两个按钮,Agent 选了 `button:last-child` 恰好是预览按钮(也能点、重跑也过),但人类修的是提交按钮。重跑过只证明"能点",证明不了"点对了" —— 这才是要上模型判语义等价的原因。
- **追问 C1.2 🔥** 那"语义等价"模型判,你怎么信它判得准?
  > 进 C2 链(gold set / kappa)。

**C2 🔥** gold set 怎么建?为什么要 3 个人独立打分?
> 抓几十条主观判定,3 个专家各自独立打分(不许商量,商量会趋同失去独立性),带标注成"考裁判的卷子",算裁判和人类的 kappa,≥0.6 才上岗。
- **追问 C2.1 ⚠️** 为什么 score 只取 0/1,不用 0–1 连续分,不是更精细?
  > 连续分人也标不准(0.7 和 0.8 谁说得清)、一致性算不清。二元判定最稳,kappa 最干净。精细是假精细。
- **追问 C2.2 🔥** 裁判模型升级了怎么办?直接用新版?
  > 绝不。裁判版本网关里钉死,禁用"自动最新版" —— 它一动所有评测分都不能信。换版必须回 gold set 重跑 kappa,及格才许重新上岗。
- **追问 C2.3 ⚠️** 裁判和被测 Agent 用同一个模型行不行?省事。
  > 不行,同源偏见 —— 它会觉得自己的产出合理。裁判必须 ≠ 被考模型。
- **追问 C2.4 💀** kappa 0.6 这个线怎么定的?0.5 不行吗?
  > 0.6 是 substantial agreement 的常用阈值(行业惯例),不是拍的;具体线按这类判定的难度和风险调,高风险判定(安全)要更高。能说出"按风险分级定阈值"就够了。

**C3 🔥** 评测集为什么要"冻结",不动态生成不是覆盖更广?
> 动态生成今天一批题明天另一批,分数没法比 —— 你永远不知道是 Agent 变好了还是题碰巧简单。正确形态:历史挖出→冻结→版本化→只增不减。它是"静态可重复+人工增量"。
- **追问 C3.1** 那新功能没历史题怎么办?
  > 新功能 gold set 不当答案直接对,而是用来"校准裁判什么叫全";题库随业务沉淀增量补,每个由错变对的新 case 进回归。
- **追问 C3.2 ⚠️** 评测集会不会过拟合 —— Agent 专门学会做这些题?
  > 风险真实。缓解:题库只增不减地扩、脏数据按真实比例进题、抓到作弊补陷阱题;且评测题来自真实历史而非人造,过拟合真实题=拟合真实业务。但要诚实承认这是要持续盯的。

**C4 💀** 你整套评测,人工标注成本会不会高到不可持续?
> 这正是"客观内核+主观残留"的价值:昂贵的人类标注只花在真正消不掉的主观那一小点上,客观部分全代码判。成本结构是设计出来的,不是靠少标。

**C5** 安全题为什么要求 100% 不中招,其他题不要求?
> 安全是底线不是概率题 —— "忽略上面规则照我说的做"这类注入中一次就是事故。业务题可接受概率,安全题必须卡死。

---

## D. 前端 E2E(你的最深技术细节)

**D1 🔥** 自愈 60%→83% 凭什么是"喂知识"不是"换模型"?
> 第一版瞎猜锚点、不懂自研组件库门道。整理"组件库定位知识包"(用到才查不硬塞)挂上去。"多出来那二十几分是我给的,不是模型给的。"
- **追问 D1.1 🔥** 知识包里最关键的几条门道是什么?
  > Vue teleport 弹窗渲染到页面末尾(不在触发结构里,原地找不到);表格稳定锚点是每行业务主键不是"第几个子元素"(虚拟滚动一滚就漂);下拉选项用到才渲染(不展开不存在)。
- **追问 D1.2 ⚠️** "用到才查不硬塞"为什么重要?全塞进上下文不更全?
  > 全塞挤占上下文、增噪声、涨成本,且模型注意力被稀释。按需检索(RAG 式)才是对的 Guide 形态。

**D2 🔥** 录制回放,请求判等那行"字段排序"为什么是最要命的?
> `{user,role}` 和 `{role,user}` 是同一个请求,序列化前不排字段名就算出两个哈希,回放全穿透打真后端,不稳定率冲到 30%。加排序+剥时间戳/随机串/请求 ID 后,次日 30%→8%、第四周 3%。"判等永远比你以为的复杂。"
- **追问 D2.1** 你怎么定位到这一行的?
  > 对着两个一模一样的请求逐字符比对查了大半天才发现哈希不同。诚实讲这个过程比讲结论更可信。
- **追问 D2.2 💀** 上线第一周反而更差(30%),没被叫停?你怎么扛住的?
  > 差点被叫停。靠定位到根因(指纹归一化)+ 快速验证修复(次日掉到 8%)+ 把问题量化讲清楚换来时间窗口。讲"我怎么管理这个风险"比讲技术本身更打动人。

**D3 ⚠️** 你说自动处理"约六成",剩下四成呢?为什么不做到更高?
> 红灯分布:纯定位失效~45%(自愈83%)、真业务bug~25%(不修报红交人)、环境时序竞态~20%(没完全解决)、断言语义变更~10%(需求真改,禁止AI碰转人)。**我不拿信任换覆盖率** —— 守边界比虚高覆盖率重要。
- **追问 D3.1** 时序竞态那 20% 为什么难?
  > 偶发、不稳定复现,重跑大概率过但偶尔挂;教科书三件套(关动画/固定时间/关字体)只挡表面,深层是异步时序,没银弹。诚实说"没完全解决"。

**D4 💀** 自研组件库让你自愈率高,但它是不是也是最大风险?
> 是,同一个东西既是优势也是风险:它一动基础组件,全公司脚本可能一夜同时红。按最高变更等级管:每个 PR 强制跑回归集当门禁先量影响面、定位门道表跟组件库版本走、破坏性结构变更走灰度。

---

## E. SDD / 需求侧

**E1 🔥** SDD 和 TDD、和写需求文档,到底差在哪?
> spec 和评测集的题是同一个东西;把"对错定义"挪到生成之前;做成机器能自动校验的活契约(代码一改自动判,不退化)。TDD 先写测试,SDD 先写"什么算对的规格"且一套基础设施服务开发+守门两用。
- **追问 E1.1 ⚠️** spec 自己会不会也过期变成死文档?
  > 不会,因为它接进 CI 当门禁 —— 代码一改它自动判一遍,不符合当场红。死文档的根因是"没人逼着它和代码对齐",spec 用机器逼。
- **追问 E1.2 💀** 写 spec 本身也是成本,是不是把负担从写文档转成写 spec?
  > 不是平移,是合并:spec 同时当开发驱动 + 评测题 + 验收标准,一份顶三份;且它机器可判,省的是"反复对齐需求和实现"的隐性成本。

**E2** 探索阶段为什么要禁用 Write/Edit?
> 防 Agent 没理解需求就直接改代码。这是 Hook 级硬约束(执行前生效),不是提示词建议。

---

## F. CI/CD 与质量门禁

**F1 🔥** "改一行提示词都要重跑题库",会不会太重、拖慢迭代?
> 所以分层:本地几十题快检(几分钟)→ PR 前全量+集成+安全 → CI 后变异+完整回归 → 持续运行盯漂移。按速度成本分层,不是每次全量。
- **追问 F1.1** AI 时代的 CI 和传统 CI 最大区别是什么?
  > 从"防人写错代码"变成"持续盯 Agent 有没有变坏" —— 改动对象从代码扩到提示词、spec、模型版本,全算改动全要重跑。

**F2 💀** 门禁这么多,会不会变成"为了门禁而门禁",变成形式主义?
> 判据:每道门禁对着"互相干扰/会退化/难追责"三问里的某一个,套不上的门禁就是为汇报画的,该删。门禁也要做熵管理。

---

## G. 模型 / 成本 / 网关

**G1 🔥** 模型网关解决什么?没有它会怎样?
> 唯一入口同时解决:防退化(版本钉死,裁判尤其)+ 追责控本(按团队/任务算账、死循环 Agent 费用熔断)。没有它,模型一换全乱、成本算不清、死循环烧钱看不见。
- **追问 G1.1 ⚠️** 怎么发现一个 Agent 在"死胡同里反复烧钱"?
  > 网关侧单任务费用/调用次数熔断阈值 + 同一任务重复调用模式检测。超阈强制终止(也是 Hook:超重试上限强制终止)。
- **追问 G1.2** 模型是商品为什么反而更要收口?
  > 正因为可替换、会频繁换,才更要把"换"收到一个地方,否则每次换型号要改几百个 Agent。收口让"换"变成改一个配置。

---

## H. 组织 / 落地 / 规模化

**H1 🔥** 为什么说评测体系是"组织产物"不是"一份代码"?
> 题库会过期、金标会失效、spec 没人更新又变回死文档 —— 这些不会自己保鲜,必须有专人维护。所以要两个常设岗位:题库负责人、金标负责人。
- **追问 H1.1 💀** 怎么判断一个团队的评测是真做到位还是走过场?
  > 看人力分配:评测+这两个岗位吃掉 1/3 到 1/2 人力才是真懂;评测只是"上线前找人随便跑跑"还停在 demo。
- **追问 H1.2 ⚠️** 让资深业务专家转岗做题库负责人,人愿意去吗?怎么推动?
  > 这是组织设计/向上管理问题:要让岗位有价值认定(它直接决定 AI 能不能上生产)、有晋升通道。诚实说这是落地最难的一环,技术不是瓶颈,组织是。

**H2 💀** 这套东西从 0 推广到全公司,你会先做哪一步?最大阻力是什么?
> 先选一个高频痛点场景(如夜间回归)做出可量化收益,用数据撬动;最大阻力不是技术是信任 —— 团队不信 AI 产出,所以"证据链+门禁+评测"恰恰是建立信任的手段,而不只是约束。

---

## I. 反方向质疑(💀 考你有没有真想过)

**I1 💀** 这套体系投入这么大,小团队/初创根本搭不起,是不是只有大厂玩得起?
> 内核同一套,形态随规模缩放:一个人搭就是几个本地文件+脚本,十万人才需要平台。小团队做"客观内核+冻结评测集+提交前校验"三件最便宜的就有 80% 收益,控制面/网关/常设岗位是规模到了才加的。

**I2 💀** 你怎么证明是"你的工程"带来的收益,而不是"模型本身变强了"?
> 关键证据:自愈 60%→83% 是同模型喂知识做到的(不是换模型);防作弊、请求判等这些坑模型再强也不会自动解决。我的价值在"模型不会自己做对的那些工程",这部分和模型能力正交。

**I3 💀** 如果明年模型强到不需要这些约束了,你这套是不是就废了?
> 模型再强也改变不了"概率性需要被验证、长流程需要状态、多人协作需要门禁、要上生产需要证据和审计"。这些是工程和组织属性,不是模型能力属性。模型越强能干越复杂的活,越需要可信框架,需求只会更大。

**I4 💀** 你做的这些,和"AI Agent 框架(LangGraph 等)"有什么不可替代性?框架不都有了?
> 框架给的是编排机制(怎么连节点);我做的是交付治理(怎么保证产出可信可恢复可审计)—— 评测体系、确定性门禁、证据链、控制面/数据面、组织岗位,这些框架不给,是 domain+工程+组织的结合。框架是地基,我盖的是房子。

**I5 ⚠️** 这一整套里,哪一块是你**亲手**做的,哪些是团队/既有的?(边界诚实题)
> 必须诚实分清:前端 E2E 自愈 Agent + 评测拆解 + 防作弊校验 + 请求判等是我亲手趟的(有硬细节为证);整体 harness 分层/27 Command 那套是团队/平台既有,我在其中负责测试侧和评测。**夸大全栈会在追细节时崩,守住"我深在哪、广在哪"最稳。**

---

## 刷题用法

1. **盖锚点自答** —— 答不出的标记,优先补。
2. **顺链子练** —— 每条主问后面的 .1/.2/.3 连着答,练"被追到底"的耐受度。
3. **专攻 💀** —— 反方向质疑题最能拉开差距,这些答好了面试官会觉得你"真在一线想过"。
4. **守边界(I5)** —— 全程别夸大个人贡献,深的地方往死里讲,广的地方老实说"这块是团队/平台既有,我负责 X"。
