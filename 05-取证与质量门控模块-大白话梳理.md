# 模块一 · 取证与质量门控｜大白话梳理

> 基于原文档和问答澄清，重新组织叙述顺序，先讲整体再拆细节，重点解释"为什么这么设计"。

---

## 1. 整体链路

一次故障在这模块里走 6 个节点：

```
triage → planner → evidence_fanout → evidence_aggregate → diagnose → critic
```

这 6 个节点之后，critic 放行才进入 remediation（修复方案），那是另一个模块的事。

---

## 2. triage — 把自然语言翻译成机器可读的故障分类

**输入：** 用户提交的工单（可能是手工填的自然语言、工单 ID、告警事件）

**做什么：** 把人类描述翻译成结构化数据

**怎么做的（三层策略）：**

1. **规则优先**：先拿关键词匹配，`发布/release/rollback` → `deployment_regression`，`cpu/memory/oom` → `resource_exhaustion`，`timeout/503/502` → `dependency_failure`。命中就直接出结果，不调模型。
2. **LLM 补充**：规则没命中才调一次 LLM，把 `title + description + service + env + severity` 喂进去，要求按 JSON schema 输出 `incident_type`、`suspected_services`、`severity`、`suggested_time_window`、`requires_immediate_human`、`rationale`。
3. **fallback 兜底**：LLM 调用失败或返回格式解析不了，降级为 `service_degradation`，保证 graph 不会因为模型失败而中断。

**输出：** `TriageResult` 对象，例如：

```
incident_type="deployment_regression"
severity="P2"
suspected_services=["payment-service"]
suggested_time_window={"start": "2h ago", "end": "now"}
requires_immediate_human=False
rationale="发布后错误率升高，疑似发布回归"
```

**为什么这么设计：** 后面的 planner 不能直接消费自然语言，必须先结构化。三层策略是为了兼顾速度（规则快）、覆盖面（LLM 补）、健壮性（fallback 兜底）。

---

## 3. planner — 按故障类型拿出固定调查清单

**输入：** `TriageResult`

**做什么：** 根据 `incident_type` 查一个预定义的调查模板，列出要查哪些工具、按什么优先级查

**怎么做的：**

- **不调 LLM**，纯确定性逻辑。
- 每种故障类型映射一组工具。比如 `deployment_regression` 的模板是：
  ```
  query_deployments → query_logs → query_k8s_pods → query_k8s_events
  → query_metrics → query_db_slow_queries → query_runbook
  ```
- 每个任务都是结构化的 `InvestigationTask`：
  ```python
  InvestigationTask(
      task_id="task_001",
      category="deployments",        # 观测域
      tool_name="query_deployments", # 具体工具名
      priority=1,                    # 优先级
      params={"service": "payment-service", "env": "staging"}
  )
  ```

**输出：** `List[InvestigationTask]`

**为什么这么设计："该查什么"是路由决策，不能交给模型临场发挥。** 模型可能漏查关键证据、查偏方向、或者每次查的不一样无法复现。确定性模板保证每次同一类型故障查的东西一致、可复现、可审计。

---

## 4. evidence_fanout — 并行派多个专家只读取证

**输入：** `List[InvestigationTask]`

**做什么：** 把 planner 列出的调查任务按观测域分组，同时派多个 specialist agent 去查

**怎么做的：**

- **并行机制：** 使用 LangGraph 的 `Send` API 实现 fan-out。多个专家同时执行，总耗时等于最慢那一路，不是所有路串行相加。
- **专家按观测域划分，不按根因划分：**
  - `k8s_agent` — 看 Pod 状态、事件、HPA、rollout
  - `db_agent` — 看连接数、慢查询、锁等待
  - `logs_agent` — 看应用异常和错误日志
  - `metrics_agent` — 看错误率、延迟、资源趋势
  - `deployments_agent` — 看发布历史和版本变化
- **每个专家内部是 ReAct 循环：** 接到任务 → 调工具 → 看结果 → 判断是否还需继续查 → 再调工具或结束。有最大工具调用轮次和超时限制。
- **只读约束：** 这个阶段只能调 `query_*` 前缀的工具，不能调任何写操作工具。
- **单路容错：** 某个专家超时或失败，只影响该路，不阻断其他专家返回。
- 每个专家返回 `SpecialistAnalysis`，里面带两个自评标记：
  - `partial=True`：还能返回结果，但内容不完整。比如日志专家因某个工具调用失败，只能返回"有 5xx 错误，具体原因未知"。
  - `truncated=True`：被外部条件强行中断。比如超时了、达到最大轮次了、上下文窗口满了。
  - 这两个标记是专家 agent 自己对自己这轮工作的自评，不是 aggregate 层事后推断的。

**输出：** `List[SpecialistAnalysis]`，每个包含 `evidence_items`（证据列表）、`confidence`（该专家自己的置信度）、`partial`/`truncated` 标记。

**为什么这么设计：**

- 并行是为了快，故障处置时间敏感。
- 按观测域划分是因为同一个根因往往需要多个观测面互相印证（比如发布回归需要 log + metric + deployment 三方都指向同一时间点）。
- 只读是为了安全，取证阶段绝对不能碰线上。
- 单路容错是因为不能因为日志工具挂了就让整个取证都失败。

---

## 5. evidence_aggregate — 汇总多专家证据，算质量分，检测跨域矛盾

**输入：** `List[SpecialistAnalysis]`

**做什么：** 把多个专家各自看到的局部证据拼成一张全局图。不产出根因。

**怎么做的（纯计算，不调 LLM）：**

**5.1 证据汇总**

- 合并所有专家的 `evidence_items`。
- 生成 `evidence_summary`（自然语言摘要）。
- 生成 `cross_agent_causal_chains`（跨专家因果链）。比如 `deployments 发现 14:00 发布 → logs 发现 14:05 报错 → metrics 发现 14:06 5xx 升高`，形成一条异常传播时间线。

**5.2 质量分计算**
质量分衡量的是"这一轮多专家取证整体够不够完整、可信"，由四个因子相乘：

| 因子           | 含义                           | 例子                                              |
| -------------- | ------------------------------ | ------------------------------------------------- |
| 专家覆盖度     | 5 类专家里有几个产出了有效输出 | 5 个全有 = 1.0，只有 1 个 = 0.2                   |
| 平均专家置信度 | 各专家自己 confidence 的均值   | 三个专家 confidence 分别为 0.9/0.7/0.8 → 均值 0.8 |
| 降级惩罚       | `partial=True` 的打折系数      | 某路只有降级分析，质量打 0.7 折                   |
| 截断惩罚       | `truncated=True` 的惩罚系数    | 某路被截断，质量打 0.5 折（比降级更重）           |

最终质量分 = 平均置信度 × 覆盖度 × 降级惩罚 × 截断惩罚。

**5.3 矛盾检测**
只有 aggregate 能同时看到多个专家视角，所以矛盾检测必须放这里。

比如：

```
logs_agent 报告：应用层大量的 NullPointerException
k8s_agent 报告：Pod 没有重启，CPU/Memory 正常，HPA 正常
```

这不叫"证据无效"，这叫**应用层异常存在，但基础设施层正常 → 根因大概率在应用层而非基础设施层**。

aggregate 把这种信号写入 `contradiction_signals`。

**输出：** `evidence_summary`、`evidence_quality_score`（0-1）、`contradiction_signals`、`cross_agent_causal_chains`

**为什么这么设计：** 证据工程和根因推理必须分离。aggregate 只负责把证据整理好、算清楚可信度、标注好矛盾，不做任何根因推断。这样 downstream 的 diagnose 拿到的输入是干净的、有质量标记的、有矛盾提示的，而不是一堆原始碎片。

---

## 6. diagnose — 基于证据生成候选根因

**输入：** `evidence_summary`、`evidence_items`、`contradiction_signals`、`incident_type`

**做什么：** 这是第一个让 LLM 深度参与推理的节点，基于 aggregate 整理好的证据包，生成 2-3 个根因假设。

**怎么做的：**

- 调一次 LLM，prompt 包含上述所有输入，要求按 JSON schema 输出候选根因列表。
- 输出 2-3 个 `RootCauseCandidate`，每个包含：
  ```
  hypothesis: "v2.8.3 发布后引入空指针异常，导致支付接口 5xx 升高"
  confidence: 0.72                                 # 置信度 0-1
  incident_type: "deployment_regression"
  supporting_evidence_ids: ["ev_deploy_001", "ev_logs_001", "ev_metrics_001"]  # 支持该假设的证据
  contradicting_evidence_ids: ["ev_k8s_001"]        # 与该假设矛盾的证据
  next_checks: ["检查新旧版本 Pod 的错误分布", "对比 v2.8.3 与上一版本的支付参数处理逻辑"]
  ```
- `next_checks` 是每个候选根因附带的验证清单，不是执行计划。含义是"如果这个根因是真的，下一步应该看到什么证据"。来源有三层：
  1. 从 supporting/contradicting evidence 反推证据缺口
  2. 按故障类型模板匹配（发布回归 → 查 diff，资源耗尽 → 查 CPU/memory）
  3. LLM 根据证据摘要补全现场相关检查项

**如何对待矛盾信号：** 矛盾信号不会阻止 diagnose 产出候选。diagnose 只是把矛盾证据写入 `contradicting_evidence_ids`，并可能降低该候选的置信度。比如上面例子中，k8s 正常是矛盾的，所以 confidence 是 0.72 而不是更高。

**为什么输出 2-3 个候选：** 表达不确定性。故障诊断很少只有一个可能原因，多个候选保留推理过程，用于审计和复盘。但后续处置只看 top-1，其余候选只记录不做动作。

**输出：** `List[RootCauseCandidate]`

**为什么这么设计：** diagnose 的角色是"基于证据提出假设"，不是"做最终裁决"。它和 critic 分工明确：一个提假设，一个判假设。这是为了杜绝"提出结论的人自己给自己放行"。

---

## 7. critic — 门控裁决，判断候选根因能不能进入处置

**输入：** `evidence_quality_score`、`contradiction_signals`、`root_cause_candidates`（含各候选的 confidence 和 contradicting_evidence_ids）、`loop_count`

**做什么：** 不重新取证，不重新诊断，只读取现有信息做路由裁决。

**怎么做的（确定性规则，不调 LLM）：**

| 裁决                       | 触发条件                                                       | 下一步                                                 |
| -------------------------- | -------------------------------------------------------------- | ------------------------------------------------------ |
| **PASS**                   | 质量分 ≥ 阈值 且 top candidate confidence ≥ 阈值 且 无严重矛盾 | 进入 remediation（修复方案）                           |
| **NEED_MORE_EVIDENCE**     | 质量分或置信度不够，但方向大致对                               | 回到 evidence_fanout 补证，loop_count + 1              |
| **REPLAN / CONTRADICTION** | 矛盾信号严重或调查方向有问题                                   | 回到 planner 重规划                                    |
| **NEEDS_HUMAN**            | loop_count ≥ 2 仍无法通过                                      | 转人工，写入 terminal_reason = EVIDENCE_LOOP_EXHAUSTED |

**循环上限默认 2 轮：** 首轮取证 + 一次纠偏。两轮之后如果还不行，说明工具接入有问题、权限缺失、观测面不足、或者这个故障本身需要人工上下文，继续循环只是浪费时间，直接转人工。

**为什么这么设计：** critic 是独立门控节点。它的存在是为了保证：**提出假设的人（diagnose）和放行假设的人（critic）不能是同一个。** 在一个 ReAct agent 里，模型自己提假设、自己觉得对、自己就往下干了，这在故障处置里是致命的风险。critic 用确定性规则做裁决，不依赖模型判断，确保"证据不够就是不够，不能蒙混过关"。

---

## 8. 关键设计决策汇总

| 设计点                   | 决策                            | 原因                                       |
| ------------------------ | ------------------------------- | ------------------------------------------ |
| triage 三层策略          | 规则优先 + LLM 补充 + fallback  | 兼顾速度、覆盖面、健壮性                   |
| planner 不用 LLM         | 确定性模板生成                  | "该查哪"是关键路由，必须可复现、可审计     |
| 取证并行 + 只读          | 多专家同时查，不碰写操作        | 快、安全、互不拖累                         |
| 专家按观测域划分         | k8s/db/logs/metrics/deployments | 不按根因划分，因为同一根因需多观测面印证   |
| 证据和根因推理分离       | aggregate 不产出根因            | 证据质量是一个独立工程问题                 |
| 质量分多维计算           | 覆盖度 × 置信度 × 降级 × 截断   | 防止单点高置信掩盖全局证据缺口             |
| 矛盾检测放 aggregate     | 只有聚合层能看跨专家冲突        | 单专家看不到其他专家的结论                 |
| diagnose 输出 2-3 个候选 | 保留不确定性                    | 单答案过于武断，多候选可审计               |
| critic 独立门控          | 不调 LLM，确定性规则裁决        | 提出假设和放行假设必须分属不同节点         |
| 循环上限 2 轮            | 首轮 + 一次纠偏                 | 避免无限消耗，2 轮不行说明系统能力边界到了 |

---

## 9. 一个完整例子

事故：**支付服务 5xx 升高，用户下单失败**

fanout 派出 4 个专家，返回：

```
logs_agent:     发现大量 NullPointerException，14:05 开始    → evidence: ev_logs_001
metrics_agent:  5xx 从 0.2% 升到 8%，p95 latency 升高       → evidence: ev_metrics_001
k8s_agent:      Pod 正常，无重启，CPU/Memory/HPA 正常        → evidence: ev_k8s_001
deployments_agent: payment-service 14:00 发布了 v2.8.3       → evidence: ev_deploy_001
```

aggregate 输出：

```
evidence_summary: "logs/metrics/deployments 异常，k8s 正常"
evidence_quality_score: 0.82
contradiction_signals: [{"anomaly": "logs", "normal": "k8s", "reason": "应用层故障，非基础设施"}]
cross_agent_causal_chains: [deployments → logs → metrics]
```

- evidence_summary：一句话概括这轮取证发现了什么——三个观测面（日志、指标、发布记录）都抓到异常，只有 K8s 层面一切正常。就是给下游一个快速概览，不需要去翻所有证据细节。
- evidence_quality_score: 0.82：这轮取证的质量分。4 个专家都回来了（覆盖率够），各自置信度不低（0.8+），没有降级也没有截断，所以整体 0.82，算靠谱。
- contradiction_signals：发现了一组矛盾——日志报了应用层异常，但 K8s 基础设施层没任何问题。这个信息告诉下游：根因大概率在应用代码或配置上，不用去查 Pod 重启、OOM、节点故障这些东西了。reason 字段明确标注了推断："应用层故障，非基础设施"。
- cross_agent_causal_chains：把多个专家的发现串成一条时间因果链——14:00 发布（deployments）→ 14:05 开始报空指针（logs）→ 14:06 5xx 飙升（metrics）。这条链说明三件事不是孤立的，而是按先后顺序、因果关系串起来的，发布是起点，报错和指标异常是后果。

diagnose 输出 2 个候选：

```
候选1: deployment_regression, confidence=0.72
  supporting: ev_deploy_001, ev_logs_001, ev_metrics_001
  contradicting: ev_k8s_001

候选2: configuration_error, confidence=0.48
  supporting: ev_logs_001, ev_metrics_001
  contradicting: (空)
```

候选 1：发布回归，置信度 0.72

- 三个证据支持它：发布记录（14:00 发了新版）、日志（报空指针）、指标（5xx 飙升）。时间线对得上，因果链完整。
- 一个证据矛盾：K8s 一切正常。说明不是基础设施问题，但跟"发布回归"这个假设本身不冲突——发布回归本来就是应用层问题，K8s 正常反而印证了这一点。所以置信度仍然有 0.72，只是没到 0.9。
  候选 2：配置错误，置信度 0.48
- 两个证据支持：日志报错、指标异常。这两个只能说明"应用出问题了"，但没法区分是代码 bug 还是配置问题。
- 没有矛盾证据——因为没有专门查配置中心的专家，所以既没人说配置改了，也没人说配置没改。contradicting: (空) 不代表没有矛盾，而是没有证据可用来判断。
- 所以置信度只有 0.48，比候选 1 低很多。本质上是"不排除这种可能，但没证据支撑"。

critic 裁决：

```
质量分 0.82 够，top candidate confidence 0.72 够
但存在矛盾信号，k8s 正常是反证
→ 不直接放行，判 NEED_MORE_EVIDENCE，要求补查新旧版本 Pod 错误分布
```

这个例子说明了核心设计理念：矛盾不是让系统停下来，而是让系统降低置信度、保留反证、要求补证。critic 就是那个决定"现在的证据够不够、能不能往前推"的角色。

---

## 10. 给新人的一句话主线

```
用户提交故障
  → triage 把自然语言收敛成结构化故障类型
  → planner 按故障类型决定查什么（确定性模板，不靠模型猜）
  → fanout 派多个专家同时只读取证
  → aggregate 汇总证据、算质量分、发现矛盾
  → diagnose 基于证据生成 2-3 个候选根因（LLM 参与，但受证据约束）
  → critic 判断候选是否可靠到可以进入处置（确定性规则，不靠模型自评）
  → 不可靠就补证/重规划/转人工
  → 可靠才放行给 remediation
```

这套设计的核心不是"让模型更会猜"，而是：**让模型在证据约束下归纳，让系统在工作流约束下放行。**
