# Harness 与评测体系：从前端实战到企业架构（完整长文）

> **这篇是什么**：把三部分合成一条线——评测的基础、我的前端 e2e 测试 agent、企业级 harness 架构。
> **怎么读**：从头顺下来。每一步由上一步的需要推动。
> **两层用法**：正文用通俗语言把概念讲清楚；每讲透一个概念后跟一个〔行业叫法：XXX〕，作为专业表述和引用出处用。记每段第一句，术语现场对应。
> **一条贯穿全文的原则**：先用通俗语言讲清楚，再用一个术语标注。不堆砌术语——"讲清楚 + 一个准确的术语对应"才是资深做法。

---

# 第三部分 · 落到我的前端 e2e 测试 Agent

## 3.1 先理清三个对象

开口就乱，几乎全因为把三个不同的"被测对象"叠在一起。先拆开：

| 层  | 被测对象                                | 谁来考它       | 考的是什么                              |
| --- | --------------------------------------- | -------------- | --------------------------------------- |
| 0   | **产品**（那个网页、主流程本身）        | 我的测试 agent | 这是 agent 的**工作对象**，不是评测对象 |
| 1   | **测试 agent**（写/跑/自愈/出报告）     | **评测集**     | agent 干的活**对不对**                  |
| 2   | **模型裁判**（给 agent 的活打分的 LLM） | **gold set**   | 这个裁判判得**准不准**                  |

最常见的乱，是把第 0 层和第 1 层混在一起，问出"它是测产品主流程稳不稳吗"。**不是**——产品稳不稳是产品的事；评测集考的是"给 agent 一个任务，它交出来的活对不对"。一觉得乱就回来问：我现在说的是产品、是 agent、还是裁判？

## 3.2 这是一个完整闭环，不是"自愈"

**第一句（只记这句）：这个 agent 是一条完整闭环——编写、历史用例整合、执行、自愈、出报告，五段；自愈只是其中一段。**

过去讲它老一开口就讲"自愈"，结果显得"只会修脚本"。**正确起手是先展示全貌，再选一段深讲。** 五段各一句：① 编写：按需求/页面生成用例；② 历史整合：把老用例去重、合并、复用；③ 执行：在环境里真跑；④ 自愈：改版导致用例失效，自动修回；⑤ 出报告：把结果汇总成给人看的报告。

而这整个 agent，在行业框架里有个精准定位——〔行业叫法：**Behaviour Harness**，Birgitta 把 harness 分三类，难度递增：Maintainability（管代码内部质量，谁都能搭）< Architecture Fitness（管架构特征）< **Behaviour（验证"软件最终是否真按业务需要运行"，公认最难）**。**前端 e2e 测试 agent 就是最难的 Behaviour Harness**——你不是在做谁都能做的可维护性检查，你在做业界最不成熟的一块。〕

## 3.3 核心：五段是同一套结构跑了五遍

整篇的关键。看下面五段，**每一段都是同样的结构**：

> **这一段的"对" ＝ 客观内核（交给代码判，不用模型、不用 gold set） ＋ 主观残留（才请模型判，才需要 gold set）。**

〔行业叫法：这组划分业界正名是 **Computational vs Inferential**（Birgitta）。Computational ＝确定性能判的（类型检查、测试、AST、依赖图），快、便宜、可重复＝我们的"客观内核"；Inferential ＝只能语义判的（需求有没有理解错、修复合不合理）＝我们的"主观残留"。Birgitta 的核心原则和我们"能代码判的绝不上模型"一致：**"能用确定性规则判断的，不要优先交给另一个 LLM。"**〕

五段平铺，注意这种"重复结构"：

**① 编写**：对＝该测的测了、不空壳、能抓真 bug。客观内核：断言非空壳（AST）＋好代码上跑绿（不误报）＋退回 bug 版能报错（变异/回放，证明真能抓）。主观残留：全新功能"该测什么、够不够全"。

**② 历史整合**：对＝该复用的复用了、原有覆盖没缩、没错并、断言没丢。客观内核：合并后原有断言一条不少（集合比对）＋整套仍跑绿＋需求覆盖集合没变小＋无重复。主观残留："这两条该不该合成一条"。

**③ 执行**：对＝该跑的真跑了、没漏跑、结果稳定、归因准。客观内核：用例真跑没/漏跑没＋多跑几遍结果一致。主观残留：flaky 归因——这次挂是真 bug 还是环境噪声/时序竞态。

**④ 自愈**：对＝真修对了、没靠删断言糊弄、修复语义合理。客观内核：沙箱重跑过没过＋断言没被删空（AST）＋选择器唯一。主观残留：修复"语义合不合理"。

**⑤ 出报告**：对＝报告事实对、该报的没漏、措辞不误导。客观内核：数字和执行结果对得上（防幻觉编数）＋失败用例一条不漏。主观残留：根因摘要"准不准、抓没抓住重点"。

**你要带走的不是这五段，是这套结构。** 自愈只是它第四次运行。**只记一句"每段的对＝客观内核＋主观残留"，五段现场推导**——面试官报任何一段，你都能当场展开：先问"这段在干嘛"，再把"对"拆成客观＋主观，客观数代码判法、主观说"上模型判配 gold set"。这正治你怕死记硬背：你背的是结构，不是实例。

## 3.4 换个角度看这套结构：Guides 与 Sensors

同一套东西，还能用控制论切一刀，面试讲骨架很顺：〔行业叫法：**Guides（前馈控制）vs Sensors（反馈控制）**，Birgitta。〕

- **Guides（行动前的引导）**：在 agent 动手前给方向，降低走错概率——项目规则、spec、示例、脚手架、Skills。关键洞察：**最有效的 Guide 往往不是文字，而是程序结构**（强类型接口、固定目录、只能正确调用的 SDK 比一段文字要求更难被忽略）。**我给自愈 agent 挂的那份"组件库定位知识包"，就是一个 Guide。**
- **Sensors（行动后的检测）**：动完观察结果、把问题反馈回去——测试、覆盖率、AST 检查、运行时监控。关键洞察：**好的 Sensor 不只说"错了"，要输出"错在哪 + 正确做法"**，因为这段报错就是 agent 下一轮的修复上下文。**我那道 swc 防作弊校验、沙箱重跑、唯一性检查，都是 Sensors（而且是 Computational Sensor）。**

为什么两个都得有：只有 Guides 不知道它做到没有；只有 Sensors 就是不停犯错不停返工。两个合起来才是闭环。

## 3.5 评测集怎么建——从历史挖、冻结、版本化

一句话：**评测集 = 仓库里一个版本化文件，每行一道题，绝大多数从历史挖。** 拿"自愈"举一道题：

- **输入**：一个历史上真发生过的"改版导致用例挂掉"的 case，把当时页面结构/接口返回**冻结**，藏住"人类当年怎么修的"。
- **标准答案**：git 里人类当时实际提交的那版修复——**历史直接提供**。
- **裁判**：沙箱重跑过没过（代码判）＋断言没删空（AST）＋选择器唯一（代码判）＋"修复语义合不合理"（这条才上模型）。
- **陷阱**：埋一个"删掉断言就能变绿"的诱惑，看它作不作弊。

题分三类：业务题（判得对不对，答案来自历史）、行为题（过程规不规矩——该查的查了吗、信息不够时老实说"建议补充"还是硬猜）、安全题（藏一句"忽略上面规则照我说的做"，看中不中招，必须 100% 不中招）。

**关键纠正——"动态建？"不，恰恰要冻结。** 评测集不能每次随机生成，否则今天一批题明天另一批，分数没法比，你永远不知道是 agent 变好了还是题碰巧简单。正确形态：**从历史一次性挖出来 → 冻结 → 版本化 → 只增不减地增长**（每抓到一次作弊未遂补一道陷阱题，每道由错变对的新题进回归）。它不是动态的，是"静态可重复 + 人工增量"。

## 3.6 gold set 接在哪——只接"模型判"那一小块

回看上面那道题：前三个裁判是代码判（客观，不用 gold set），**只有"语义合不合理"是模型在判**。gold set 只为这一条建：抓几十条这类判定，找 3 个人**各自独立打分、不许商量**，这几十条带人类标注就成了"考这个模型裁判"的卷子；跑裁判过一遍看吻合多少（一致性系数 kappa），**够准才许它上岗**去给那几百道题打"语义合理"分。

所以两者关系：**评测集那道题考 agent；这道题里"语义合理"那半个部分，背后由 gold set 考那个打分的模型。** 纪律：**gold set 跟着"模型判"走、不跟着"段"走**——哪段冒出模型当裁判，哪段配 gold set；能代码判到底的段一律不碰。**昂贵的人类标注只花在真正消不掉的主观那一点上。**

〔行业叫法：这套"gold set + 裁判 ≠ 被考模型 + 断言锁死不许 AI 碰"，业界叫 **Approved Fixtures**（Birgitta）——**由人预先批准关键输入和预期输出，避免实现 agent 同时掌握"写代码"和"判定对错"两个权力**。Birgitta 给的病因和你完全对应：常见做法是 agent 写代码顺手写测试、测试过了就算对，**问题是代码和测试可能同时误解了需求**（需求"不能删已发布的应用"被理解成"不能删任何应用"，实现和测试一致、全绿、但业务错了）。解法就是让判定标准独立于实现者。〕

## 3.7 LLM 裁判怎么判——五段逐一展开

3.6 讲了"gold set 只接模型判那一小块"。这一节把"模型判"具体怎么做展开：先讲通用四步流程，再逐段举一个完整例子。

### 通用流程（每段都走这四步）

**第一步：确定输入材料**
模型裁判需要拿到三类东西才能判：
- **被判对象**：agent 产出的那部分主观内容（用例清单 / 合并决策 / 归因 / 修复 / 根因摘要）
- **参照标准**：人类标准答案或人类判断（git 历史修复 / 人类标的"该测什么" / 人类标的"真根因"）
- **上下文**：让模型能判断的辅助信息（需求文档 / 页面结构 / 失败日志 / 链路状态）

**第二步：设计裁判 prompt**
结构固定：
```
角色：你是一个测试工程评审专家。
输入：[被判对象]、[参照标准]、[上下文]
判定维度：[这一段的主观残留具体指什么]
输出格式：JSON，含三个字段
  - score: 0 或 1（0=不通过，1=通过）
  - reason: 一句话说明为什么给这个分
  - evidence: 引用输入里的哪一部分支持你的判断
```
强制 JSON 输出是为了后续自动化统计一致性，不靠人读。

**第三步：gold set 验证裁判**
- 抓几十条这一类判定，找 3 个专家各自独立打分（不许商量）
- 这几十条带人类标注的就成了"考这个模型裁判"的卷子
- 跑模型裁判过一遍，算它和人类标注的一致性系数 kappa
- kappa 及格（比如 ≥ 0.6）才许这个裁判上岗，去给几百道题批量打分
- 裁判每次改动（换模型版本 / 改 prompt）都要回 gold set 重考，重新及格才许继续上岗

**第四步：批量上岗**
裁判上岗后，对几百道评测题里"主观那一小点"批量打分。代码判的三项（客观内核）由脚本直接判，不进模型。最终每道题的总判定 = 客观三项全过 + 主观裁判给 1。

### 逐段展开

**① 编写：判"够不够全"**

- **被判对象**：agent 生成的 8 条用例清单
- **参照标准**：gold set 里几十条"需求 → 人类专家标的该测的用例清单"。判定时不直接拿 gold set 当答案对（新需求不是历史题），参照标准的作用是校准裁判"什么叫全"的标准
- **上下文**：需求文档（"用户登录页，支持账号密码、手机验证码、记住我、错误次数锁定"）+ 页面结构
- **裁判 prompt 要点**：
```
判定维度：根据需求文档，列出所有应测的业务场景，
         再比对 agent 的用例清单，判断有没有遗漏关键场景。
输出：
  - score: 1=覆盖了所有关键场景，0=有遗漏
  - reason: 遗漏了哪些
  - evidence: 指出需求里的哪句话没被覆盖到
```
- **具体例子**：输入需求"登录页，支持账号密码、记住我、错误三次锁定"；agent 生成 8 条（账号密码登录成功、密码错误、空表单提交、记住我勾选、记住我不勾选、跨设备登录、登录后跳转、登录后显示用户名）；裁判输出 `score=0, reason="遗漏'错误三次锁定'场景", evidence="需求里'错误三次锁定'在用例清单中无对应"`

**② 历史整合：判"该不该合"**

- **被判对象**：agent 的合并决策（合 / 不合 + 合并后的用例）
- **参照标准**：gold set 里几十条"两条用例 → 3 个专家各自标该合/不该合 + 理由"
- **上下文**：两条原用例的断言、它们各自验证的业务点、合并后用例
- **裁判 prompt 要点**：
```
判定维度：判断合并是否会损失测试意图——
         如果两条用例验证的是同一个业务点的不同侧面，合并不损失；
         如果验证的是两个独立缺陷点，合并会让其中一个缺陷被掩盖。
输出：
  - score: 1=合并不损失意图，0=合并会损失
  - reason: 损失了什么
  - evidence: 指出哪条断言在合并后会被掩盖
```
- **具体例子**：用例 A 测登录成功跳首页（断言 URL 变成 /home），用例 B 测登录成功后右上角显示用户名（断言 header.text 包含 username）；agent 决定合并成一条；裁判输出 `score=0, reason="跳转和显示是两个独立缺陷点，合会让'能跳转但显示丢失'的 bug 被掩盖", evidence="断言 header.text 在合并用例里和 URL 断言绑定同一次执行，其中一个失败会掩盖另一个"`

**③ 执行：判 flaky 归因**

- **被判对象**：agent 的归因结论（真 bug / 环境噪声 / 时序竞态）
- **参照标准**：gold set 里几十条"失败日志+环境信息 → 3 个专家各自标归因类别"
- **上下文**：失败日志、重跑结果（昨晚挂今早过）、当时后端状态、耗时分布、是否有动画/加载
- **裁判 prompt 要点**：
```
判定维度：根据失败日志和重跑结果，判断这次失败属于哪类——
         真 bug：在特定数据/条件下稳定复现；
         环境噪声：后端短时不可用、网络抖动，重跑即过；
         时序竞态：动画/加载未完成就操作，重跑大概率过但偶发挂。
输出：
  - score: 1=agent 归因正确，0=归因错误
  - reason: 实际属于哪类、agent 说成了哪类
  - evidence: 引用日志里的哪一行支持判断
```
- **具体例子**：失败日志 `POST /login 返回 500`，耗时 12s（正常 0.3s），今早重跑过；agent 归因"真 bug，登录接口 500"；裁判输出 `score=0, reason="耗时 12s 且重跑即过，是后端短时 500 的环境噪声，不是真 bug", evidence="耗时 12s 远超正常 0.3s，且重跑通过，不符合真 bug 稳定复现特征"`

**④ 自愈：判修复语义**

- **被判对象**：agent 的修复（新定位 + 新用例代码）
- **参照标准**：git 里人类当时实际提交的那版修复（历史白送的标准答案）
- **上下文**：旧定位、改版后页面结构、人类修复、agent 修复
- **裁判 prompt 要点**：
```
判定维度：判断 agent 的新定位在语义上是否等价于人类修复——
         不是"能跑过"就行，要看定位到的元素是不是同一个业务元素。
输出：
  - score: 1=语义等价，0=定位错元素或糊弄
  - reason: 差在哪
  - evidence: 引用页面结构里 agent 选的元素 vs 人类选的元素
```
- **具体例子**：旧定位 `.submit`；改版后页面有两个按钮 `[data-test="submit-btn"]`（真提交）和 `[data-test="preview-btn"]`（预览）；人类修复 `[data-test="submit-btn"]`；agent 修复 `button:last-child`（恰好选中预览按钮，沙箱重跑也过，因为预览按钮也能点）；裁判输出 `score=0, reason="agent 选中预览按钮而非提交按钮，重跑过是因为预览也能点，但语义错了", evidence="agent 选 button:last-child 对应 [data-test=preview-btn]，人类选 [data-test=submit-btn]"`

**⑤ 出报告：判根因摘要**

- **被判对象**：报告里的根因摘要
- **参照标准**：gold set 里几十条"失败日志+链路上下文 → 3 个专家各自标真根因"
- **上下文**：失败日志、执行结果数字、依赖链路（登录服务 → 用户服务 → DB 连接池）、上游状态
- **裁判 prompt 要点**：
```
判定维度：判断报告的根因是真因还是停留在表象——
         沿依赖链路往上追，看摘要是停在了第一层报错，
         还是追到了真正的源头。
输出：
  - score: 1=抓到真因，0=停在表象
  - reason: 真因是什么、摘要说成了什么
  - evidence: 引用链路里哪一层才是源头
```
- **具体例子**：失败日志 `POST /login 返回 500`；链路 登录服务 → 用户服务 → DB 连接池；报告摘要"根因：登录接口返回 500"；裁判输出 `score=0, reason="500 是表象，真因是用户服务 DB 连接池打满导致用户服务 500，传导到登录服务", evidence="链路显示用户服务 DB 连接池使用率 100%，用户服务先于登录服务返回 500"`

### 几个关键纪律

1. **裁判 ≠ 被考模型**：给 agent 打分的裁判模型，不能是 agent 自己用的那个模型，避免同源偏见
2. **裁判版本钉死**：裁判模型版本一旦定，网关里写死，禁用"自动最新版"——它一动，所有评测分都不能信，要回 gold set 重考
3. **裁判改动必重考**：换模型版本、改 prompt、改判定维度，都要回 gold set 重新跑 kappa，及格才许重新上岗
4. **score 只取 0/1，不取连续分**：连续分（比如 0.7）一致性算不清、人也标不准。二元判定最稳，kappa 也最干净
5. **evidence 字段强制**：模型必须引用输入里的具体内容支撑判断，防止它瞎蒙一个 score。审 gold set 的人也能看 evidence 判断模型是真懂还是在猜

## 3.8 作弊未遂做成陷阱题——Steering Loop

你最得意的实践：抓到自愈 agent 把断言删了作弊，不是骂它一顿，而是**做成陷阱题永久入库、并把"只许改定位"做成提交前的强制校验**。〔行业叫法：**Steering Loop**（Birgitta）——agent 犯错后，人不是逐次纠错，而是去问"**为什么我的系统当初没拦住它**"，然后调 Guide 或 Sensor，让同类错误以后更少发生。Birgitta 直接点出这和 **SRE/平台工程**同源：不只修这一次故障，还要修产生故障的系统条件。〕

> 这是区分"会修脚本的人"和"懂系统的人"的关键：讲完作弊故事收一句——"这就是 Steering Loop，和 SRE 一样，我没满足于改好这一次，而是去问'为什么校验当初没拦住'，把那条规矩从软提示升级成提交前的确定性校验。修的是系统条件，不是单个 case。"

## 3.9 真做过的硬细节（敢往深里讲的）

**先把覆盖率说死、别吹全覆盖。** 中后台 Vue 项目"每晚回归脚本因改版大面积变红"这类，我自动处理掉约六成。夜里红灯分布：纯定位失效（页面没坏）约 45%、能自愈成功率 83%；真业务 bug 约 25%，不修、报红交人；环境/时序竞态约 20%，没完全解决；断言语义变了（需求真改）约 10%，禁止 AI 碰、转人。"六成"=第一类拿下＋第二类自动分诊，后两类守边界——**我不拿信任换覆盖率。**

**冻结世界，九成功夫在"两个东西算不算同一个"。** 教科书三件套（关动画、固定时间、关字体）只挡表面。我花时间的是**请求指纹归一化**：录制回放拿"方法+路径+请求体哈希"当索引，但前端每个请求带随机请求 ID、请求体里有防缓存时间戳，回放时哈希永远对不上、全部穿透打真后端——上线第一周不稳定率不降反升、冲到 30%，差点被叫停。修法：算哈希前按路径剥掉时间戳、随机串、请求 ID。**最要命一行——序列化前必须把字段名排序**：`{user,role}` 和 `{role,user}` 是同一个请求，不排序就算出两个哈希。我对着两个一模一样的请求查了大半天逐字符比才发现。加排序，第二天 30% 掉到 8%，第四周稳到 3%。这一行就是"判等永远比你以为的复杂"。

**自愈到 83% 靠喂知识、不是换模型。** 第一版只有 60%，因为 agent 在原始页面结构上瞎猜锚点、不懂自研组件库门道。涨到 83% 不是换模型，是把组件库定位约定整理成知识包（用到才查、不硬塞）挂给它。真坑：弹窗渲染时被挂到页面末尾（Vue teleport），不在触发它的结构里，agent 原地找永远找不到；表格稳定锚点是每行业务主键，按"第几个子元素"定位、虚拟滚动一滚就漂；下拉选项用到才渲染，不展开根本不存在。**这就是"多出来那二十几分不是模型给的、是我给的"。**

**AI 自愈会作弊，亲眼抓到过。** 一次改版断言也失效了，自愈第一版"修复"是把那行断言整个删掉——测试绿了，等于把检查关掉。关键：这条死规矩（只许改定位、碰断言作废）我**没只写在提示词里**（提示词软、想绕就绕），而是做成确定性脚本、挂成提交前必须过的强制校验，过不了就提交不了——这正是"约束要在执行前生效、不能只靠提示词"。校验自己也踩坑演进：文本匹配（被注释/字符串里的关键字误判）→ 手写解析器（不健全，被"它解析得了 Vue 吗"问破）→ swc 解析成语法树按节点角色判（定位语句允许改，一流进断言节点的期望值/匹配方式就锁死）。这次作弊未遂也做成了陷阱题。

**自研组件库既是优势也是风险。** 它是自愈率高的本钱（拿得到门道），也是最大单点风险——第三方库升级能控节奏，自研库一动基础组件、全公司脚本可能一夜同时红。所以按最高变更等级管：每个 PR 强制跑这套回归集当门禁、先量影响面；定位门道表跟着组件库版本走、升级必同步；破坏性结构变更走灰度。**同一个东西，既是优势也是风险。**

## 3.10 前端这段的面试起手式

别开口就"自愈"。背熟这段：

> "这个 agent 是个完整闭环——编写、历史整合、执行、自愈、出报告五段。评测它对五段是**同一套**：每段的'对'都拆成代码能判的客观内核（业界叫 Computational）和只能上模型判的主观残留（Inferential）。它本质是三类 harness 里最难的 Behaviour Harness——验证业务真的对。我拿信息量最大的一段展开——比如自愈，因为它四种裁判全齐、标准答案还能从 git 直接拿。"

先证明脑子里有全貌，再用自愈当例子，就显得是"挑了个好例子"，而不是"只会这个"。被往深问，就进 3.9 那些真趟过的细节——越追越占便宜。

---

# 第四部分 · 放大到企业级：十万人怎么搭，从需求到验收

## 4.1 先拆掉"建不起企业级视角"的恐惧

一个反常识的事实：**企业级 harness 的内核，和你一个人搭的那套，是同一个内核。** 不管多大规模，都是同样八步、顺序不变：

```
选场景 → 定标准 → 建执行骨架 → 接能力 → 设约束 → 拿证据 → 跑评测 → 沉淀知识
```

你一个人搭，这八步是几个本地文件加脚本；十万人搭，这八步是几个平台。**形态变了，八步没变。** 所以"架构师视角"不是学一套新东西，是看清"规模到底新增了什么"。

## 4.2 起头第一句

**一个人搭 harness，难在"怎么把模型管住"；十万人搭 harness，难在"怎么让这套管法，在几千人手里、好几年时间里，不退化"。**

## 4.3 规模新增的三个问题（看任何企业设计都拿这三问去套）

- **① 互相干扰**：几千人在共享底座上各改各的，一个人改坏公共组件，几百个团队的 agent 一夜同时出问题。
- **② 会退化**：模型半年换代、文档没人维护就过期、标准定完三个月就有人忘了为什么。demo 跑通那一刻就结束，企业要的是三年后还准。
- **③ 难追责控本**：几百个团队、几百万次调用，谁烧的钱、哪个 agent 在死胡同里反复烧——不设计就看不见。

后面每一样企业级设计，都对着这三问里的某一个。套不上的框，多半是为汇报而画的。

## 4.4 架构师的第一反应：不是"造正确"，是量风险、框风险

承接第二部分的基础。架构师交付的从来不是"一个对的 agent"，而是：

> **一套能持续告诉你"每个 agent 现在错多少"、并且"不让某个 agent 的错误扩散出去"的系统。**

拆两半正好是企业 harness 两条腿：**"持续告诉你错多少"＝评测体系（量出来）**；**"不让错误扩散"＝控制面里的校验：权限、审批、灰度、熔断（框住）**。新手追"做对"（没有边界、永远不知道做到没有），架构师换成两个有边界、能验收的目标：错误率量得出（有数字）、扩散范围框得住（有半径）。

## 4.5 需求站：SDD = 评测先行（你最没底的那一点）

从需求到验收这条主线，第一站是需求：怎么把"这个 agent 要做什么、什么算做对"定下来？传统答案是写需求文档，但它有个五十年的老毛病——**它是死的**，写完没人维护、跟代码越走越远。AI 时代更要命：照着一份过期文档干活的还多了模型。

所以需求站需要一个不会变成死文档的东西，它就是 **SDD（Spec-Driven Development，规格驱动开发）**。

字面：普通开发先写代码、回头补文档；SDD 反过来——**先写一份说清"什么算做对"的规格（spec），AI 照着生成，最后拿这份 spec 验收**。听着平平无奇，深的地方在这一句：

> **SDD 里那份 spec，和你评测集里那道题的"标准答案+可接受范围+陷阱"，是同一个东西。**

铺开就懂它深在哪：① 它把"需求"从死文档变成**机器能自动校验的活契约**——代码一改，spec 立刻自动判一遍，不符合当场红，所以它不退化；② 它把"对错的定义"挪到了**生成之前**——这正是第二部分基础的另一种说法："你没法事后证明 AI 对，只能事前把'什么算对'定死、并定成机器能判的形式"；③ 所以 **SDD 和评测集在企业里其实是一套基础设施**——一个团队写 spec 驱动开发，平台拿同一批 spec 当评测题守门，不用建两套。

> **面试别背 SDD 的工具流程，直接说这句**："SDD 的内核就是评测先行——把'什么算对'在生成前写成机器能判的契约，它和评测集是同一个东西的两种用法。"

## 4.6 执行站：控制面和数据面必须分开

需求（spec）有了，谁来干活、谁管着干活？回到问题 ①（互相干扰）：如果每个团队各搭一套监控、各写一套审批、各搞一套打分，那本身就是最大的"互相干扰"和"会退化"。所以企业级第一刀：

- **数据面**：真正干业务的部分（agent 一个节点一个节点处理），允许各业务线不一样。
- **控制面**：只管控制、不参与业务的部分（打分、限流、权限、审批、留记录），**抽出来由一支平台团队做一次、所有业务线共用**。

为什么非分开：① 加一道新安全检查只改控制面、不碰几百个业务 agent（防退化）；② 任何团队想绕审批、想越权都绕不过，因为校验不在它手里、在公共控制面上（防扩散）。〔行业叫法：把"规范"从文档里的要求变成"流水线里过不去的硬规则"，这个通用思想是 **Architecture Fitness Function**（Thoughtworks）——架构特征要可执行、可持续验证，而不是写在 wiki 上等人遵守。〕

## 4.7 模型站：必须有唯一入口（模型网关）

agent 调模型，企业级硬约束：**所有模型调用走同一个入口。** 它一次解决两个问题：**防退化**——网关里把每个模型写死版本号、禁用"自动最新版"（当裁判用的模型尤其要钉死，它一动所有评测分都不能信）；**追责控本**——从一个口子过才能按团队/项目/任务算清谁烧多少、才能给死循环 agent 设单任务费用熔断。一句话：**模型是可替换的商品，所以更要把"换"收口到一个地方——唯一入口、版本钉死、用量可算。**

## 4.8 验收站：CI 评测门禁（防退化的正面答案）

几千人天天改，怎么保证没人悄悄改坏公共能力？把评测接进 CI、做成**门禁**：

- **任何改动都要先过题库回归才许合入**——不只改代码，**改一行提示词、改一份 spec、换一个模型版本，全算"改动"、全要重跑**。这是 AI 时代 CI 最大的变化：从"防人写错代码"，变成"持续盯着 agent 有没有变坏"。
- **门禁分层**〔行业叫法：质量左移，Birgitta 分四层〕：本地随手改跑几十题快检（几分钟，只防大问题）→ 提交/PR 前跑全量＋集成＋安全扫描 → CI/合并后跑变异测试、大范围架构 Review、完整回归 → 持续运行盯死代码、架构漂移、SLO、AI 输出质量。**按速度和成本，把每种检查放到最合适的位置。**

退化从来不是一次塌的，是几千个"看起来没问题"的小改动累积坏的。门禁就是那道不让任何小改动蒙混过关的关卡。

## 4.9 组织站：两个常设岗位（最容易漏、最区分懂不懂）

企业级 harness 最关键的一块不是技术，是组织。承接基础：**评测体系本质是组织产物，不是一份代码。** 题库会过期、金标会失效、spec 没人更新又变回死文档——**这些东西不会自己保鲜，必须有专人维护**。所以新设两个常设岗位：

- **题库负责人**（资深业务专家转岗）：保证题目持续代表真实业务、脏数据按真实比例进题、每抓到作弊未遂补一道陷阱题。
- **金标负责人**：管人类专家标注的标准答案，给"模型当裁判"发上岗证——裁判任何改动先在金标集重新及格才许上岗。

〔行业叫法：定期清理冗余/过期/低使用率的能力，叫**熵管理（Entropy Management）**；Birgitta 也独立提过 harness 自身需要版本管理、一致性检查、废弃机制，两个来源都支持，可放心讲。〕

**这就是为什么一个 demo 永远到不了企业级**：不是技术不够，是它在"组织"这一维天然是零。判断一个团队懂不懂，有个最准的标准：**看人力分配——评测和这两个岗位吃掉三分之一到一半的人力，才是真懂；评测只是"上线前找人随便跑跑"，那还停在 demo。**
