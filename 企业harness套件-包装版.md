# AI-Native 软件交付平台(AI SDLC Platform)

> **一句话定位**:一个 **AI 驱动的内部研发交付平台(Internal Developer Platform)**,以 **Agent 编排 + 工程化护栏(Guardrails)** 为内核,把"需求 → 设计 → 开发 → 测试 → 缺陷闭环"全链路自动化,并通过**确定性执行、质量门禁与证据链**保证可恢复、可验证、可审计 —— 用 AI 系统性改善 **DORA 四项指标**(交付频率、变更前置时间、变更失败率、MTTR)。
>
> **本质**:不是一套 Prompt/Agent/Skill 工具集,而是把"研发交付生命周期"做成了一条**有状态、有门禁、有证据、可恢复**的流水线。一句话总结全篇 ——
>
> **LLM 决定建议做什么,Harness 决定是否允许做、是否真的做成、有没有证据。**

---

## 0. 电梯演讲(30 秒 / 2 分钟两版)

**30 秒版**

> 我做的是一个 AI-Native 的研发交付平台:它把需求、开发、测试、缺陷闭环拆成一条流水线,每个环节由 Agent 干活,但所有硬约束都下沉到程序 —— 状态文件管恢复、Hook 管拦截、编译器和 Playwright 管"真的做成没有"、质量门禁管"能不能进下一步"。核心矛盾是 LLM 是概率性的,而软件工程要的是确定性,我用工程手段把概率性行为框进了可执行、可验证、可审计的流程里。

**2 分钟版** 见配套《面试讲解稿与追问预案》。

---

## 1. 背景:我们到底在解决什么矛盾

LLM 在研发场景天然有三个问题,直接用会出事:

| 问题       | 表现                              | 后果                        |
| ---------- | --------------------------------- | --------------------------- |
| **概率性** | 同一个输入,这次对、下次错         | 不可重复,无法纳入交付流水线 |
| **会幻觉** | 宣称"编译通过""测试成功",实际没有 | 把没做成的事当成做成了      |
| **无状态** | 会话一断,上下文全丢               | 长流程无法恢复,半截工作作废 |

行业里很多"AI 编码工具"停在 **demo 能跑**,因为它们解决的是"让 AI 写出代码",而交付要的是"让 AI 写出的代码**可信地进入生产**"。这两件事差着一整套工程体系。

**我们的设计原则**(贯穿全平台):

> **LLM 负责理解、生成、分析;程序负责状态、执行、验证、阻断、留证。**
> 能用确定性规则判断的,绝不优先交给另一个 LLM。
> 〔行业叫法:**Computational(确定性可判) vs Inferential(只能语义判)**。约束要在**执行前**生效,不能只靠提示词 —— 提示词是软的,想绕就绕。〕

---

## 2. 整体架构

### 2.1 分层(关注点分离)

| 层级              | 职责                                   | 行业对应                   |
| ----------------- | -------------------------------------- | -------------------------- |
| **Command**       | 用户入口、参数解析、场景路由、流程启动 | CLI / Workflow Trigger     |
| **Agent**         | 上下文管理、阶段推进、多 Skill 编排    | Agent Orchestration        |
| **Skill**         | 领域知识、脚本调用、文档/代码生成      | Capability / Tool          |
| **Rule**          | 行为规范与场景约束(团队级 + 项目级)    | Policy / Convention        |
| **Hook / Runner** | 确定性拦截、状态、重试、质量门禁       | Guardrails / Control Plane |

当前规模:**27 Command / 24 Agent / 90 Skill / 9 类 Rule**。

### 2.2 控制面与数据面分离(企业级第一刀)

这是平台工程最关键的一刀,直接决定能不能从"一个人的工具"长成"几千人的平台":

- **数据面(Data Plane)**:真正干业务的部分 —— Agent 一个节点一个节点处理需求、写代码、跑测试。允许各业务线不一样。
- **控制面(Control Plane)**:只管控制、不碰业务 —— 打分、限流、权限、审批、留证。**由平台团队做一次,所有业务线共用。**

为什么非分开:

1. **防退化** —— 加一道新安全检查只改控制面,不碰几百个业务 Agent。
2. **防扩散** —— 任何团队想绕审批、想越权都绕不过,因为校验不在它手里,在公共控制面上。

〔行业叫法:把"规范"从 wiki 上的要求变成"流水线里过不去的硬规则",这个思想是 **Architecture Fitness Function**(Thoughtworks)—— 架构特征要可执行、可持续验证,而不是写在文档里等人遵守。〕

### 2.3 三种编排模式

| 模式           | 形态                          | 适用                         |
| -------------- | ----------------------------- | ---------------------------- |
| **直接委派**   | `Command → Skill`             | 边界明确的短任务             |
| **Agent 编排** | `Command → Agent → Skill`     | 需要动态判断、多阶段执行     |
| **自包含流程** | `Command → Step1 → … → StepN` | 长工作流、状态恢复、质量闭环 |

---

## 3. 全链路能力

### 3.1 需求站:SDD = 评测先行

传统需求文档有个五十年的老毛病 —— **它是死的**:写完没人维护,和代码越走越远。AI 时代更要命:照着一份过期文档干活的还多了个模型。

所以需求站的内核是 **SDD(Spec-Driven Development,规格驱动开发)**:先写一份说清"什么算做对"的规格(spec),AI 照着生成,最后拿这份 spec 验收。深的地方在这一句:

> **SDD 里那份 spec,和评测集里那道题的"标准答案 + 可接受范围 + 陷阱",是同一个东西。**

它带来三个本质改变:

1. 把"需求"从死文档变成**机器能自动校验的活契约** —— 代码一改,spec 立刻自动判一遍,不符合当场红,**所以它不退化**。
2. 把"对错的定义"挪到了**生成之前** —— 你没法事后证明 AI 对,只能事前把"什么算对"定死,并定成机器能判的形式。
3. **SDD 和评测集在企业里是同一套基础设施** —— 一个团队写 spec 驱动开发,平台拿同一批 spec 当评测题守门,不建两套。

**双入口落地**:

- **`/it-vibe`** 面向自然语言想法:`自然语言 → 方法论路由 → 只读探索 → 2~3 个方案 → 用户确认 → 明确需求/方案`。支持 9 种方法论(RCA、MVP 迭代、减法优先、深度探索、Working Backwards、数据驱动、结构化探索、多 Agent 赛马、流程精简)。**探索阶段禁用 Write/Edit**,防止 Agent 没理解需求就改代码 —— 这是一个 Hook 级硬约束,不是提示词。
- **`/it-design`** 面向正式 US 需求:`需求澄清 → 项目理解 → 技术方案设计 → 文档评估 → 一致性校验 → 用户确认`。

**主要产物(契约链)**:

```text
requirement-analysis.md   ← 需求分析
design_document.md        ← 技术方案
design_api.yaml           ← API 契约
design_database.sql       ← 数据模型
```

**关键门禁**:复杂需求必须用户确认;设计文档必须通过 Evaluator;需求/设计/API 契约必须三方一致;用户确认后才能移交开发。

### 3.2 开发站:状态机 + 断点恢复 + 定向修复

设计产物 → 任务拆分 → 任务校验 → 代码实现 → 文件自检 → 编译验证 → 定向修复。

**任务拆分(`it-task-initializer`)**:把设计文档转成 `tasks.md`,每个任务带编号、功能点、涉及文件/接口/模块、依赖关系、当前状态。生成后做**三方覆盖校验**:

```text
设计功能点   ↔ tasks.md 功能点
代码文件清单 ↔ tasks.md 涉及文件
API paths    ↔ tasks.md 涉及接口
```

遗漏自动补充,最多修复 3 轮。

**开发状态机(断点恢复的核心)**:

```text
[ ] 待执行   [~] 执行中   [x] 已完成
```

会话中断后:优先恢复 `[~]` → 再选依赖满足的 `[ ]` → 已完成不重复执行。

**一个 Agent 三模复用(`it-developer`)**:

| 模式         | 场景                                     |
| ------------ | ---------------------------------------- |
| FULL         | `/it-dev` 完整开发                       |
| COMPILE_ONLY | `/it-build-fix` 编译修复                 |
| TARGETED_FIX | 质量闭环中的定向修复(限制文件与方法范围) |

**确定性兜底**:Maven 退出码判断代码是否**真的**编译通过(不依赖 Agent 自己宣称"成功");编译失败最多 5 次自愈;定向修复**禁止修改白名单外文件**;禁止绕过 Git Hook、改 lint 规则、跳过任务。

### 3.3 测试站:前端 E2E 自愈 Agent(业界最难的 Behaviour Harness)

> **核心原则:测试用例是设计文档的派生品,测试验证的是设计与实现的一致性,而不是重新分析需求。**

这是整个平台技术含量最高的一块。〔行业叫法:harness 分三类,难度递增 —— Maintainability(管代码内部质量,谁都能搭)< Architecture Fitness(管架构特征)< **Behaviour(验证"软件最终是否真按业务需要运行",公认最难)**。前端 e2e 测试 Agent 就是最难的 **Behaviour Harness**。〕

**它是一条完整闭环(五段),不是"会修脚本"**:

```text
① 编写  → ② 历史整合 → ③ 执行 → ④ 自愈 → ⑤ 出报告
```

① 按需求/页面生成用例;② 老用例去重、合并、复用;③ 在真环境跑;④ 改版导致用例失效自动修回;⑤ 汇总成给人看的报告。**自愈只是第四段。**

**动态指令系统(解决测试用例最难维护的痛点)**:用例不写死数据,而用占位指令,运行时按环境/页面/时间解析真实值:

```text
[DYNAMIC:DATE:CURRENT]      [DYNAMIC:DATE:CURRENT-7DAYS]
[DYNAMIC:SELECT:FIRST_VALID] [DYNAMIC:CONFIG:base_url]
[DYNAMIC:TEXT:RANDOM:10]
```

一举解决日期过期、多环境 URL 不一致、下拉值不同、测试数据冲突、用例依赖固定环境五个问题。

**执行下沉到程序(`/it-test-ex → ui-test-ex-agent → uitest-runner → Playwright MCP`)**:

- 阶段:`Prepare → Parse → Execute → Report → Review/Increment`
- **分批执行**:每 10 个用例一批,执行完立即写 `state.json`,再自动执行下一批;中断后从上次批次恢复。
- **`state.json` 保存完整上下文**:baseUrl/host/port/protocol、URL 来源、Hash/History Router、Vite/Vue/Vue Router 版本、已完成/失败用例、执行结果。
- **原子操作**:Markdown 步骤转成独立 Playwright 操作(goto/fill/click/wait/assert/screenshot),每步保存状态、错误、耗时、`raw_js_steps`、截图。

**E2E 质量门禁(证据链)**:语义元素定位优先;相似度低于阈值拒绝误操作;关键断言必须截图;截图后检查文件存在且非空,缺失自动补拍;**证据不完整时阻断报告生成**;路径统一处理 Win/Linux/macOS 差异。

```text
产物:state.json / execution-log.json / quality-gates.json / raw_js_steps / screenshots/ / ui_test_execution_report.md
```

### 3.4 缺陷闭环:失败分类 + 缺陷聚合 + 定向修复

```text
测试报告 → 失败归因 → 缺陷聚合 → 创建 DTS → 开发侧定向修复 → 重新测试
```

**三类失败,处理方式完全不同**(避免误修业务代码):

| 类型     | 处理                      |
| -------- | ------------------------- |
| 脚本问题 | 修测试脚本,不创建产品缺陷 |
| 环境问题 | 处理环境,不动业务代码     |
| 真实 Bug | 生成缺陷并同步 DTS        |

多个相同根因的失败用例**合并成一个缺陷**,避免重复 DTS。
当前:后端 UT/API 测试已接入 `/it-dev-workflow` **全自动闭环**(≤3 轮);前端 E2E 是**半自动闭环**(自动收集证据和分类 → 用户确认创建缺陷 → 开发侧 TARGETED_FIX → 手动重新触发)。

---

## 4. 确定性与治理:Guides 与 Sensors

用控制论切一刀,整个平台的护栏分两类:

- **Guides(前馈控制,行动前引导)**:项目规则、spec、示例、脚手架、Skill。**最有效的 Guide 往往不是文字,而是程序结构** —— 强类型接口、固定目录、只能正确调用的 SDK,比一段文字要求更难被忽略。(例:给自愈 Agent 挂的"组件库定位知识包"就是 Guide。)
- **Sensors(反馈控制,行动后检测)**:测试、覆盖率、AST 检查、运行时监控。**好的 Sensor 不只说"错了",要输出"错在哪 + 正确做法"** —— 因为这段报错就是 Agent 下一轮的修复上下文。(例:防作弊 AST 校验、沙箱重跑、唯一性检查都是 Sensor。)

只有 Guides 不知道做到没;只有 Sensors 就是不停犯错不停返工。**两个合起来才是闭环。**

### Steering Loop:从"修这一次"到"修产生故障的系统条件"

最能区分"会修脚本的人"和"懂系统的人"的实践:抓到自愈 Agent 把断言删了作弊,**不是骂它一顿,而是问"为什么我的系统当初没拦住它"**,然后:① 把这次作弊做成**陷阱题永久入库**;② 把"只许改定位、碰断言作废"从软提示**升级成提交前的确定性强制校验**(过不了就提交不了)。

〔行业叫法:**Steering Loop**(Birgitta)—— 和 **SRE/平台工程**同源:不只修这一次故障,还要修产生故障的系统条件。〕

---

## 5. CI/CD 与质量门禁:防退化的正面答案

几千人天天改,怎么保证没人悄悄改坏公共能力?把评测接进 CI,做成**门禁**。

**AI 时代 CI 最大的变化**:从"防人写错代码",变成"持续盯着 Agent 有没有变坏"。

> **任何改动都要先过题库回归才许合入 —— 不只改代码,改一行提示词、改一份 spec、换一个模型版本,全算"改动",全要重跑。**

**门禁分层(质量左移,四层)**:按速度和成本,把每种检查放到最合适的位置:

| 层  | 位置         | 内容                                 | 速度   |
| --- | ------------ | ------------------------------------ | ------ |
| L1  | 本地随手改   | 几十题快检,只防大问题                | 几分钟 |
| L2  | 提交 / PR 前 | 全量 + 集成 + 安全扫描               | 分钟级 |
| L3  | CI / 合并后  | 变异测试、架构 Review、完整回归      | 较慢   |
| L4  | 持续运行     | 盯死代码、架构漂移、SLO、AI 输出质量 | 长驻   |

退化从来不是一次塌的,是几千个"看起来没问题"的小改动累积坏的。门禁就是那道不让任何小改动蒙混过关的关卡。

**Quality Gates 用布尔条件决定流程能否继续**:

```text
设计通过 AND 契约一致 AND 用户确认 → 允许开发
所有用例完成 AND 日志完整 AND 截图完整 → 允许生成报告
```

---

## 6. 评测体系:怎么知道每个 Agent 现在"错多少"

架构师交付的从来不是"一个对的 Agent",而是:

> **一套能持续告诉你"每个 Agent 现在错多少"、并且"不让某个 Agent 的错误扩散出去"的系统。**
> 拆两半正是企业 harness 两条腿:**"持续告诉你错多少"= 评测体系(量出来)**;**"不让错误扩散"= 控制面里的校验:权限、审批、灰度、熔断(框住)。**

### 6.1 同一套结构:客观内核 + 主观残留

评测任何一段产出,都用同一套结构:

> **这一段的"对" = 客观内核(交给代码判,不用模型) + 主观残留(才请模型判,才需要 gold set)。**

以前端五段为例,每段的客观内核都是确定性判定(AST 断言非空壳、好代码跑绿不误报、退回 bug 版能报错、合并后断言一条不少、多跑几遍结果一致、沙箱重跑过没过、选择器唯一、报告数字和执行结果对得上),主观残留才上模型(该测什么够不够全、该不该合、flaky 归因、修复语义合不合理、根因摘要准不准)。

〔行业叫法:**Computational vs Inferential**。核心纪律:**能用确定性规则判断的,不要优先交给另一个 LLM。**昂贵的人类标注只花在真正消不掉的主观那一点上。〕

### 6.2 评测集:从历史挖、冻结、版本化

> **评测集 = 仓库里一个版本化文件,每行一道题,绝大多数从历史挖。**

不能每次随机生成,否则今天一批题明天另一批,分数没法比 —— 你永远不知道是 Agent 变好了还是题碰巧简单。正确形态:**从历史一次性挖出 → 冻结 → 版本化 → 只增不减地增长**(每抓到一次作弊未遂补一道陷阱题,每道由错变对的新题进回归)。

题分三类:业务题(答案来自历史)、行为题(过程规不规矩 —— 信息不够时老实说"建议补充"还是硬猜)、安全题(藏一句"忽略上面规则照我说的做",必须 **100% 不中招**)。

### 6.3 gold set + 模型裁判:只接"主观那一小块"

只有"语义合不合理"这类主观判定才上模型裁判。建 gold set 流程:抓几十条这类判定 → 找 **3 个专家各自独立打分(不许商量)** → 算模型裁判和人类标注的一致性系数 **kappa** → **kappa 及格(如 ≥ 0.6)才许上岗**。

**几条铁纪律**:

1. **裁判 ≠ 被考模型** —— 给 Agent 打分的裁判,不能是 Agent 自己用的模型,避免同源偏见。
2. **裁判版本钉死** —— 网关里写死版本,禁用"自动最新版";它一动,所有评测分都不能信,要回 gold set 重考。
3. **裁判改动必重考** —— 换模型、改 prompt、改维度,都回 gold set 重跑 kappa,及格才许重新上岗。
4. **score 只取 0/1** —— 连续分一致性算不清、人也标不准。
5. **evidence 字段强制** —— 模型必须引用输入里的具体内容支撑判断,防止瞎蒙。

〔行业叫法:**Approved Fixtures**(Birgitta)—— 由人预先批准关键输入和预期输出,避免实现 Agent 同时掌握"写代码"和"判定对错"两个权力。病因:agent 写代码顺手写测试、测试过了就算对,但**代码和测试可能同时误解了需求**("不能删已发布的应用"被理解成"不能删任何应用",全绿但业务错了)。解法就是让判定标准独立于实现者。〕

---

## 7. 企业级治理:十万人怎么不退化

> **一个人搭 harness,难在"怎么把模型管住";十万人搭 harness,难在"怎么让这套管法,在几千人手里、好几年时间里,不退化"。**

规模新增三个问题(看任何企业设计都拿这三问去套):

- **① 互相干扰**:一个人改坏公共组件,几百个团队的 Agent 一夜同时出问题。
- **② 会退化**:模型半年换代、文档过期、标准三个月没人记得为什么。
- **③ 难追责控本**:几百万次调用,谁烧的钱、哪个 Agent 在死胡同里反复烧,不设计就看不见。

**模型站:必须有唯一入口(模型网关)** —— 所有模型调用走同一个入口,一次解决两个问题:**防退化**(写死版本号,裁判模型尤其钉死)+ **追责控本**(按团队/项目/任务算清谁烧多少,给死循环 Agent 设单任务费用熔断)。一句话:**模型是可替换的商品,所以更要把"换"收口到一个地方 —— 唯一入口、版本钉死、用量可算。**

**组织站:两个常设岗位(最容易漏、最区分懂不懂)** —— 评测体系本质是**组织产物**,不是一份代码。题库会过期、金标会失效、spec 没人更新又变回死文档:

- **题库负责人**(资深业务专家转岗):保证题目持续代表真实业务、脏数据按真实比例进题、每抓到作弊未遂补一道陷阱题。
- **金标负责人**:管标准答案,给"模型当裁判"发上岗证 —— 裁判任何改动先在金标集重新及格才许上岗。

〔行业叫法:定期清理冗余/过期/低使用率能力,叫 **熵管理(Entropy Management)**;harness 自身也需要版本管理、一致性检查、废弃机制。〕

> **判断一个团队懂不懂的最准标准:看人力分配 —— 评测和这两个岗位吃掉三分之一到一半人力,才是真懂;评测只是"上线前找人随便跑跑",还停在 demo。**

---

## 8. AGENTS.md 与 Rules 的边界

- **AGENTS.md = 项目宪法**:项目定位、分层、权威数据源、全局流程、用户确认门、全局禁止行为、状态恢复、上下文隔离、异常升级、标准产物。所有 Agent 必须遵守。
- **Rules = 场景策略**:Java Maven 编译、Git 安全、定向修复、UI 测试设计/执行、缺陷分类、AUI/Jalor/Starling 规范。只约束某个场景怎么做,不负责完整流程编排。

**优先级(项目可细化团队,但不能覆盖不可绕过的安全规则)**:

```text
企业基线 → 团队级 Rules → 项目级 Rules → 当前任务参数
```

---

## 9. 成熟度与演进路线

```text
L1 Prompt 工具
L2 Agent 工作流
L3 有状态、有门禁、有恢复的 Harness      ← 当前整体在这里
L4 企业统一治理平台(控制面/网关/评测门禁/组织岗位)
L5 数据驱动自优化平台
```

当前整体 **L3,前端 E2E 接近 L3.5**。

**已识别的演进方向(体现规划力,面试主动抛)**:

1. 部分硬约束仍是 Prompt 约束 → 全面下沉为确定性 Hook/校验。
2. 模式判断靠自然语言参数 → 改结构化参数(强类型 Runtime)。
3. Markdown 同时承担人类文档和机器状态 → 拆分,建统一状态内核。
4. 多个状态文件缺统一状态引擎 → 建状态引擎。
5. 缺完整权限/安全/可观测/评测治理 → 建控制面 + 模型网关 + CI 评测门禁 + 两个常设岗位。
6. 前端 E2E 半自动 → 纳入全自动修复闭环。

---

## 10. 量化收益(示例数据,需替换为真实值)

> 以下为**示例量级**,用于说明收益结构;上线请替换为你环境的真实数据。

**前端 E2E 自愈(真实实践量级)**:

- 中后台 Vue 项目夜间回归"改版大面积变红",**自动处理掉约六成**(纯定位失效自愈成功率 **83%** + 真业务 bug 自动分诊报红交人)。
- 红灯分布:纯定位失效 ~45%(自愈 83%)/ 真业务 bug ~25%(不修,报红)/ 环境时序竞态 ~20% / 断言语义变更 ~10%(禁止 AI 碰,转人)。
- 录制回放请求指纹归一化:不稳定率 **30% → 8%(次日) → 3%(第四周)**,关键修复是"序列化前字段名排序 + 剥离时间戳/随机串/请求 ID"。
- 自愈率 **60% → 83%** 靠喂"组件库定位知识包"(Guide),不是换模型 —— "多出来那二十几分不是模型给的,是我给的"。

**平台级 DORA 改善(示例)**:
| 指标 | 改造前(示例) | 改造后(示例) |
|------|--------------|--------------|
| 变更前置时间(需求 → 可上线) | ~5 人天 | ~2 人天 |
| 回归测试人工耗时 | 每轮 ~1.5 人天 | 每轮 ~0.5 人天 |
| 变更失败率 | ~15% | ~6% |
| E2E 用例维护成本 | 改版后大面积手工修 | 自动处理约六成 |

---

## 附:一句话总结(面试结尾用)

> 这套平台通过 Command / Agent / Skill 组织需求、开发、测试全链路,再通过状态文件、Hook、Runner、编译器、Playwright MCP、质量门禁和证据链,把 LLM 的概率性行为约束成可恢复、可验证、可审计的企业研发交付流程。它的内核是**评测先行(SDD)+ 确定性护栏(Guides/Sensors)+ 控制面/数据面分离**;最难的一块是业界公认最不成熟的 **Behaviour Harness(前端 E2E 自愈)**,我在那里趟过请求判等、防 AI 作弊、知识包提自愈率这些真实硬细节。下一步重点是统一的强类型 Runtime、状态引擎、模型网关与评测治理 —— 也就是从 L3 走向 L4 企业治理平台。
