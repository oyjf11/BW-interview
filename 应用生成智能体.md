# AI 应用生成平台 · 技术汇报书

> 代号：**Atlas**（暂定）
> 一句话定义：用户用自然语言描述需求，平台通过 AI Agent 自动生成、运行、迭代并部署一个真实可用的应用（Web / 小程序 / 内部工具）。

---

## 0. 摘要（Executive Summary）

Atlas 是一个对话驱动的 AI 应用生成平台。用户无需编写代码，通过自然语言对话 + 实时预览 + 可视化微调，即可得到一个结构正确、可运行、可持续迭代的应用，并一键部署。

产品的核心不是"生成一段代码"，而是构建一个能够**感知工程环境 → 规划 → 调用工具执行 → 验证 → 自我修正**的工程化 Agent 闭环。我们把"代码工程本身"作为唯一可信状态源，Agent 的智能体现在这个闭环的稳健性上，而非靠对话历史去记忆项目。

关键技术决策有四点。第一，单 Agent 强闭环为主、多 Agent 按需引入，避免过早陷入多 Agent 协同的复杂度。第二，分层上下文 + 代码语义检索，用"文件树摘要 + 按需读取 + 局部 diff"控制 token 成本与出错率。第三，模型分级路由，旗舰模型负责规划与复杂生成，自研/开源模型负责高频低难度任务，以平衡质量与成本。第四，服务端 microVM 沙箱 + 浏览器内即时预览双轨，兼顾隔离安全与交互体验。

本文从产品方案、技术架构、Agent 内核、代码生成与执行、技术选型、工程挑战、数据飞轮、安全合规、可观测性到演进路线，给出完整方案。

---

## 1. 产品方案

### 1.1 产品定位与目标用户

Atlas 定位为"自然语言到应用"的生产力平台，服务三类由浅入深的用户。

第一类是**业务与运营人员**，他们没有编程能力，需要快速搭建营销页、活动落地页、内部表单、数据看板等轻应用。这是规模最大、付费意愿与频次最稳定的基本盘。第二类是**产品经理与设计师**，他们需要把想法快速变成可交互原型，用于验证、评审与演示，对"从描述到可点击"的速度极度敏感。第三类是**独立开发者与专业工程师**，他们把 Atlas 当作加速器，用于搭建项目骨架、生成样板代码，并要求能够导出代码、接管后续开发。

服务好前两类是商业基本盘，服务好第三类是技术口碑与生态的来源。产品设计需要在"足够简单，零代码可用"和"足够开放，专业可控"之间提供平滑的能力阶梯。

### 1.2 核心价值主张

对用户而言，Atlas 的价值在于把"想法到可用应用"的周期从数天压缩到数分钟，并且在迭代阶段保持低门槛——用户始终用自然语言描述"想改成什么样"，而不是去理解代码结构。与传统低代码拖拽工具相比，Atlas 不受限于预设组件库，能够生成真正灵活的界面与逻辑；与纯代码生成工具相比，Atlas 提供了完整的运行、预览、部署闭环，交付的是"可用的应用"而非"一段代码"。

### 1.3 产品形态

核心界面由四个区域构成：左侧的**对话区**承载意图输入与迭代指令；中间的**实时预览区**即时渲染当前应用，所见即所得；右侧的**代码/文件视图**面向专业用户，可查看与直接编辑工程；以及一个**可视化微调层**，允许用户在预览上直接点选元素进行轻量修改（改文案、调颜色、换图片），无需走对话。

交付能力上，支持在线托管运行、绑定自定义域名、以及完整源码导出（面向需要接管的开发者）。

### 1.4 核心用户旅程

典型旅程是一个螺旋上升的迭代环。用户首先用一句话或一段话描述需求；Atlas 进行意图澄清（必要时反问关键缺口，但不过度打断），随后生成应用骨架并在数十秒内呈现可运行的首版预览；用户基于预览提出修改（"把顶部导航改成深色""加一个登录页""数据从这个接口拉"），Atlas 增量修改并重新验证；如此往复，直至满意后部署。

整个旅程的设计原则是**每一轮都有可见、可运行的产出**，避免让用户长时间面对黑盒等待。

### 1.5 差异化

相对于市场上的同类产品，Atlas 的差异化锚点在于三处。一是迭代体验的稳健性——大多数同类产品在第一版生成时表现尚可，但在多轮迭代后容易"越改越乱"，根因在于对项目状态的记忆失真，Atlas 以"代码即真相"的工程方案正面解决这一痛点。二是中国本土场景的深度适配，尤其是**微信小程序生成**这一高需求、高壁垒的能力。三是与平台生态（支付、地图、账号、数据服务等）的开箱即用集成，让生成的应用能直接对接真实业务能力。

### 1.6 产品边界（MVP 与远期）

MVP 阶段聚焦"前端为主的 Web 应用 + 简单后端逻辑 + 数据库托管"，把对话生成、实时预览、增量迭代、一键部署这条主链路打磨到可靠。

远期逐步扩展到小程序生成、复杂后端与工作流、第三方服务集成市场、团队协作与版本管理，最终演进为"自然语言软件工厂"。明确不在早期范围内的是：高度定制化的企业级复杂系统、需要深度架构设计的大型应用——这些应作为"加速专业开发"而非"替代专业开发"来定位。

---

## 2. 整体技术架构

### 2.1 架构总览

系统自上而下分为六层。

**接入层**负责用户连接与会话管理，核心是支撑流式输出的长连接（WebSocket），以及鉴权、限流、路由。

**应用编排层（Orchestration）**是系统的大脑调度中枢，维护每个会话的任务状态机，决定何时调用 Agent、何时执行工具、何时验证、何时向用户流式返回，并管理长任务的异步化。

**Agent 内核层**是智能的核心，包含上下文组装、规划、工具调用决策、记忆读写、自我修正逻辑。这一层是后文第 3 节的重点。

**执行沙箱层**为每个会话提供隔离的运行环境，承担代码的构建、运行、测试、预览。安全隔离与启动速度是这一层的核心矛盾。

**数据与存储层**持久化项目文件、工程元数据、会话状态、用户偏好、向量索引与代码索引。

**模型层**通过统一的模型网关对接多个大模型（自研、第三方旗舰、开源），并实现分级路由、缓存、降级。

### 2.2 核心数据流

一次典型迭代的数据流如下。用户指令经接入层进入编排层；编排层触发 Agent 内核；内核首先从沙箱实时读取当前工程的文件树与相关代码（重建项目认知），从记忆系统读取用户意图与偏好，组装上下文；内核调用模型生成"修改计划 + 具体的文件 diff"；编排层把 diff 应用到沙箱中的工程，触发构建与验证；若验证报错，错误信息回流到内核进入自我修正循环；验证通过后，预览更新并流式呈现给用户，同时把本轮的决策摘要写入会话记忆。

### 2.3 关键设计原则

贯穿整个架构的设计原则有五条。

**代码即真相**：项目的真实状态永远以沙箱中的工程文件为准，Agent 每次行动前从环境重建认知，绝不依赖几十轮前对话里的自我描述，从根本上规避状态幻觉。

**验证闭环优先**：模型产出代码不等于任务完成，必须经过构建/lint/运行验证，并具备依据报错自我修正的能力。这是生成质量的命门。

**增量优于重写**：优先输出局部 diff 而非整文件重写，降低成本、出错率，并天然支持版本回滚。

**上下文隔离**：当引入子任务或多 Agent 时，各自在隔离的上下文中工作，仅回传结构化结论，避免上下文互相污染。

**成本与质量分级**：不同难度的任务路由到不同等级的模型，把昂贵的旗舰算力用在刀刃上。

---

## 3. Agent 内核设计

### 3.1 Agent 运行模型

内核采用 **Plan-and-Execute 与 ReAct 相结合**的运行模型。对于一个用户请求，先由规划阶段产出一个高层修改计划（要动哪些部分、按什么顺序），再进入执行阶段逐步实施，每一步遵循"思考 → 调用工具 → 观察结果"的 ReAct 循环，并在每步后判断是否需要修正或继续。

之所以不用纯粹的单步 ReAct，是因为应用生成往往涉及多文件、多步骤的协调，先规划能让执行更有条理、更可控、也更易于向用户解释正在做什么；之所以也不用纯粹的静态计划，是因为执行中常遇到预期外的报错，需要 ReAct 式的动态调整能力。

### 3.2 上下文工程

上下文按四层动态组装，而非每轮全量塞入。

**系统层**常驻，包含平台能力边界、技术栈约束、代码规范、输出格式契约（如 diff 格式）。**项目层**描述当前工程，关键技巧是优先提供"文件树 + 每个文件的导出符号/职责摘要"，而非全部源码——模型先看结构，决定要改哪个文件，再按需读取该文件全文。**任务层**是当前这一轮的用户意图与澄清结果。**检索层**是从代码索引中临时召回的相关片段（"用户说的登录按钮在哪个文件"）。

几个常驻的**结构化锚点**尤为重要：设计 token（颜色、间距、字体规范）、路由表、数据 schema、组件 props 约定。把这些固化为结构化文本常驻上下文，避免模型每轮自行"发明"一套，导致前后界面不一致。

当对话变长，工作记忆采用**滚动摘要**：将早期轮次压缩为结构化摘要（做了哪些决策、改了哪些文件、待办事项），仅保留最近若干轮原文。

### 3.3 工具系统

工具按职能分为四组。

**文件操作类**是最高频的工具，包括读文件、写文件、局部替换（精确的 str_replace 式编辑）、列目录、删除/重命名。**执行验证类**包括运行构建、执行 lint、跑测试、启动预览服务、读取运行时报错——这组工具构成了自我修正的事实基础。**检索类**包括代码库的语义搜索与符号搜索，用于在大型工程中精确定位。**外部能力类**包括 Web 搜索（查最新库用法）、读取设计稿、图片生成、连接数据库与平台服务（支付、地图、账号等）。

工具粒度的设计是关键权衡：太细会导致调用次数爆炸与高延迟，太粗会导致失败时难以定位。Atlas 采用"计划-执行"分段，配合中等粒度的文件级操作，在可控性与效率间取得平衡。每个工具调用都被结构化记录，用于可观测与回放。

### 3.4 记忆系统

记忆系统严格区分三类，因为它们的生命周期完全不同。

**工作记忆**是当前会话本轮迭代的来龙去脉，通过滚动摘要管理，存于会话级缓存（Redis）。

**项目记忆**是这个应用的事实——技术栈、文件结构、已建功能、数据模型、设计规范。这是最关键也最容易做错的一层：它**不应依赖对话历史，而应从工程本身实时派生**。代码即真相。任何行动前从文件系统重建项目认知，从而避免"以为某组件还在、其实早被删了"这类幻觉。

**长期/用户记忆**是跨项目的用户偏好（代码风格、常用技术栈、UI 审美倾向），需用户授权，做成显式偏好配置加上从历史行为提炼的画像，存于持久化数据库。

一条贯穿的实践原则：**能从环境实时读到的，绝不存入记忆**。记忆只保存无法重建的东西——用户的意图、偏好、以及历史决策背后的理由。

### 3.5 多 Agent 协同

是否引入多 Agent 本身是一个权衡。Atlas 的立场是：**单 Agent 强闭环为主，多 Agent 按需引入**。单 Agent 配合优秀的工具循环已能解决大部分场景，多 Agent 主要用于处理复杂度与并行度。

具体采用**编排者 + 子 Agent**的分层模式。一个 Orchestrator 维护全局状态与任务队列，将可独立的子任务（如"实现前端组件""配置数据库""接入支付")分发给在隔离上下文中工作的 Sub-agent；子 Agent 完成后仅回传结构化结论与接口契约，而非大段原始对话。这种上下文隔离正是多 Agent 的核心价值——让每个 Agent 的上下文更聚焦、prompt 更专精，同时避免相互污染。

多 Agent 的三个难点对应三项机制。状态一致性：多个 Agent 并发改代码会冲突，通过文件级锁或写入串行化解决。通信成本：Agent 间只传结构化结论与契约，控制 token 与延迟。错误传播：Orchestrator 负责识别、隔离子 Agent 的错误并重试，防止单点错误污染全流程。

### 3.6 自我修正与验证回路

这是决定产品"智能程度"体感的核心环节。模型生成 diff 后，编排层应用变更并执行构建与 lint；若失败，将精确的错误信息（报错类型、文件、行号、堆栈）回灌给 Agent，Agent 据此定位并生成修复 diff，再次验证。该循环设有最大重试次数与策略升级机制——多次小修无效时，升级到更强模型或回退到更大范围的重构。

验证不仅看"能否编译运行"，还应在条件允许时进行轻量的运行时检查（页面能否正常渲染、关键交互是否报错）。验证回路做得好不好，直接拉开产品之间的质量差距。

---

## 4. 代码生成与执行

### 4.1 项目脚手架与模板

新建应用不从空白开始，而是基于精心维护的**脚手架模板**：预置技术栈、目录结构、设计系统、常用组件与最佳实践配置。这既保证了生成结果的工程规范性与一致性，也大幅降低了模型的负担和出错概率。模板按场景分类（落地页、管理后台、电商、数据看板、小程序等），模型基于意图选择起点。

### 4.2 增量编辑

迭代阶段一律走**局部 diff**而非整文件重写。这要求上下文携带被改文件的精确当前状态，否则 diff 会对不上。系统对 diff 的应用做严格校验：定位锚点唯一、变更可逆。每次成功的 diff 都进入版本历史，支持任意回滚——这对"越改越乱"时的恢复至关重要。

### 4.3 执行沙箱

每个会话拥有隔离的执行环境。Atlas 采用**双轨策略**：轻量、即时的交互（前端预览、快速反馈）走浏览器内的容器化运行（WebContainer 思路），实现近乎零延迟的预览；较重的工作负载（完整构建、后端运行、依赖安装、潜在不可信代码执行）走服务端的 **microVM 沙箱**（Firecracker，并以 gVisor 等加固），在强隔离与可控启动速度间取得平衡。沙箱具备资源配额、网络管控、生命周期管理。

### 4.4 实时预览

预览是产品体验的核心。前端运行结果通过沙箱即时反映到预览区，变更后增量热更新而非整页刷新，保证迭代的流畅感。预览区同时承载可视化微调层，支持点选元素回写到代码。

### 4.5 部署

部署采用容器化 + 平台托管的方案（K8s 编排），用户一键即可将应用发布到平台分配的域名或自定义域名，背后处理构建、产物存储、CDN 分发、HTTPS。对需要接管的开发者，提供完整源码导出。

---

## 5. 技术选型

下表给出关键技术组件的选型、理由与备选方案。原则是：核心链路（编排、Agent 内核、沙箱）倾向自研以掌控状态与可观测性，外围能力优先选用成熟方案。

| 领域                    | 选型                                      | 理由                                  | 备选                      |
| ----------------------- | ----------------------------------------- | ------------------------------------- | ------------------------- |
| 平台前端                | React + TypeScript + Tailwind             | 生态成熟、团队熟悉、组件丰富          | Vue / Svelte              |
| 代码编辑器              | Monaco Editor                             | VS Code 同源，专业编辑体验            | CodeMirror                |
| 实时通信                | WebSocket（流式）                         | 支撑 token 级流式与双向交互           | SSE（仅单向）             |
| 编排引擎                | 自研状态机                                | 生产级需精确掌控状态/重试/可观测      | LangGraph（可借鉴其思路） |
| 生成应用栈 (Web)        | Next.js + Tailwind + 组件库               | 全栈一体、生态完善、易托管            | Remix / Vite SPA          |
| 生成应用栈 (小程序)     | Taro / 原生小程序 DSL                     | 覆盖微信生态高需求场景                | uni-app                   |
| 大模型（规划/复杂生成） | 第三方旗舰模型                            | 复杂推理与代码能力上限高              | 自研旗舰                  |
| 大模型（高频低难度）    | 自研 / 开源模型（Qwen、DeepSeek 等）      | 摘要、分类、简单 diff 等任务降本      | 蒸馏小模型                |
| 模型网关                | 自研统一网关                              | 分级路由、缓存、降级、限流一处管控    | LiteLLM                   |
| 执行沙箱（重）          | Firecracker microVM + gVisor              | 强隔离 + 可控启动速度                 | gVisor 单独 / 容器        |
| 执行沙箱（轻）          | 浏览器内容器（WebContainer 思路）         | 近零延迟预览体验                      | 服务端预览                |
| 代码语义检索            | 向量索引 + 嵌入模型                       | 自然语言定位代码                      | 纯关键词检索              |
| 代码符号检索            | tree-sitter 解析 AST                      | 精确的符号级定位                      | ctags                     |
| 向量库                  | pgvector（起步）→ Milvus/Qdrant（规模化） | 起步简单、规模化时迁专用库            | Pinecone                  |
| 元数据存储              | PostgreSQL                                | 事务可靠、生态成熟                    | MySQL                     |
| 工程文件存储            | 对象存储（S3 兼容）                       | 海量文件低成本、可扩展                | 分布式文件系统            |
| 会话状态缓存            | Redis                                     | 低延迟、支持 TTL                      | Memcached                 |
| 异步任务队列            | RocketMQ / Kafka                          | 长任务解耦、削峰、可靠投递            | RabbitMQ                  |
| 容器编排                | Kubernetes                                | 行业标准、弹性伸缩                    | 自建调度                  |
| 可观测                  | OpenTelemetry + LLM 专用追踪              | 全链路 + Agent step/工具/token 级追踪 | 商用 APM                  |

---

## 6. 关键工程挑战与应对

**成本控制**。大模型调用是主要成本。应对手段是模型分级路由（昂贵算力只用于规划与复杂生成）、上下文精简（文件树摘要 + 按需读取，避免全量塞入）、prompt 缓存（系统层与项目层稳定部分缓存）、以及局部 diff 而非整文件重写。

**延迟体验**。生成是耗时操作，关键是不让用户面对黑盒等待。应对手段是 token 级流式输出、计划先行（让用户即时看到"打算做什么"）、轻量预览走浏览器内零延迟通道、长任务异步化并实时反馈进度。

**生成质量与幻觉**。这是产品成败的核心。应对手段是脚手架降低自由度、代码即真相杜绝状态幻觉、强制验证闭环 + 自我修正、结构化锚点保持一致性。

**安全隔离**。运行用户/模型生成的代码存在风险。应对手段是 microVM 强隔离、资源配额、网络管控、以及对生成代码的安全扫描。

**高并发**。海量会话同时运行沙箱对资源是巨大压力。应对手段是沙箱池化与复用、闲置回收、按需弹性伸缩、以及轻重负载分流。

---

## 7. 数据飞轮与模型策略

Atlas 的长期壁垒来自数据飞轮。平台运行中持续沉淀宝贵数据：用户的原始意图、生成的代码、迭代修改的轨迹、验证报错与修复过程、用户最终接受或放弃的结果。这些数据是优化产品和模型的金矿。

模型策略分三步走。短期以"第三方旗舰模型 + 精心 prompt 工程 + RAG（检索最佳实践与组件示例）"快速达到可用质量。中期基于沉淀数据，对高频任务（如 diff 生成、报错修复、意图理解）训练或微调专用小模型，在这些场景上以更低成本达到甚至超过通用模型的效果，并完善评估体系（生成成功率、首次可运行率等离线评测集）。长期投入自研旗舰能力，将平台数据优势转化为模型能力优势，形成"数据越多 → 模型越强 → 产品越好 → 用户越多 → 数据越多"的正循环。

---

## 8. 安全与合规

安全维度涵盖代码执行安全（沙箱隔离、资源与网络管控、生成代码安全扫描）、数据安全（用户工程与数据的隔离存储与加密、访问控制）、以及内容合规（对生成内容的合规审查，防止生成违规应用或内容）。

隐私与合规上，明确用户数据的使用边界，数据用于模型训练需获得授权并支持退出，遵循相关数据保护法规。对接平台支付、账号等敏感能力时，遵循对应的安全规范与资质要求。

---

## 9. 可观测性与质量度量

整条链路必须可观测、可回放。基于 OpenTelemetry 构建全链路追踪，并对 Agent 的每一步——规划、每次工具调用、每次模型请求、每轮验证——做结构化记录，做到任意会话可复盘、任意失败可定位。

核心质量指标包括：**首次可运行率**（首版生成即能正常运行的比例，衡量初始生成质量）、**迭代收敛轮次**（用户达到满意所需的平均迭代次数，越低越好）、**自我修正成功率**（验证失败后自动修复成功的比例）、**单会话成本**（平均 token 与算力消耗）、**端到端延迟**（指令到预览更新的时间）、以及业务侧的**部署转化率**与**留存**。这些指标既驱动工程优化，也是模型迭代的北极星。

---

## 10. 演进路线图

| 阶段                 | 时间    | 核心目标                                             | 关键交付                                                               |
| -------------------- | ------- | ---------------------------------------------------- | ---------------------------------------------------------------------- |
| Phase 1 · 主链路     | 0–3 月  | 跑通"对话 → 生成 → 预览 → 迭代 → 部署"，Web 应用为主 | 单 Agent 强闭环、脚手架模板、双轨沙箱、流式预览、一键部署              |
| Phase 2 · 质量与体验 | 3–6 月  | 提升迭代稳健性与生成质量，完善可视化微调             | 代码语义检索、自我修正强化、滚动摘要记忆、可视化点选编辑、用户偏好记忆 |
| Phase 3 · 能力扩展   | 6–12 月 | 拓展场景与生态，引入多 Agent                         | 小程序生成、复杂后端/工作流、平台服务集成、编排者+子 Agent 协同        |
| Phase 4 · 数据飞轮   | 12 月+  | 沉淀数据训练专用模型，构建壁垒                       | 高频任务专用模型、完善评测体系、团队协作、集成市场                     |

---

## 11. 风险与总结

主要风险有三。技术风险在于多轮迭代的稳健性是行业公认难题，需要"代码即真相 + 验证闭环"的方案被工程化落实，而非停留在设计。成本风险在于大模型调用成本可能侵蚀利润，需要模型分级与上下文优化持续见效。竞争风险在于赛道拥挤且头部产品迭代快，差异化（小程序、本土生态、迭代稳健性）必须真正成立。

总结而言，Atlas 的核心竞争力不在于"能否生成代码"——这已是行业基线——而在于**能否在多轮迭代中持续保持可靠**。实现这一点的关键，是把代码工程本身作为唯一可信状态源，让 Agent 的智能体现在"感知环境 → 规划 → 工具执行 → 验证 → 自我修正"这个闭环的稳健性上；记忆只保留无法从环境重建的意图与偏好；多 Agent 仅在确需隔离上下文或并行时才引入。在此工程哲学之上，叠加本土场景的深度适配与数据飞轮，构成 Atlas 的长期壁垒。
